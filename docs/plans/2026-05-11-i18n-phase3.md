# i18n Phase 3 実装プラン — Admin UI を next-intl で JA + EN 対応

> **For agentic workers:** REQUIRED SUB-SKILL: Use `superpowers:subagent-driven-development` (recommended) or `superpowers:executing-plans` to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

- **Date**: 2026-05-11
- **Status**: Draft (要ユーザー承認)
- **Spec**: `docs/specs/2026-05-10-i18n-strategy-design.md`
- **Epic**: #109
- **Phase 3 Issue**: #107
- **Phase 2b PR (参考)**: #126 (merged)

**Goal:** Admin UI (`app/`) を `next-intl` で完全に i18n 化し、JA + EN の 2 言語で動作可能にする。Phase 2b で導入した API code ベース訳出 (`app/src/lib/api-errors.ts`) をそのまま catalog に統合し、Phase 2 以前から残っていた UI 直書き日本語をすべて catalog へ移管する。

**Architecture:** Next.js 16 App Router + `next-intl` (Server Components 内で `getTranslations()`、Client Components で `useTranslations()`)。`proxy.ts` (Next.js 16 で `middleware.ts` から rename) にロケール検出ロジックを追加。`users.preferred_locale` カラムで永続化、Cookie + Accept-Language でフォールバック。

**Tech Stack:** `next-intl` 4.x、Next.js 16 App Router、React 19、ICU MessageFormat、Vitest (missing-key テスト)、eslint-plugin-i18next (試験導入)。

---

## 0. Survey: 着手前の現状

- **Next.js 16** (`app/package.json` 確認済み)、`app/src/proxy.ts` で既存の auth routing 中
- **next-intl は未インストール**
- **`users.preferred_locale` カラムは未追加** (要 migration、`docker/postgis-init/04_auth_schema.sql` に追加 OR 新規 migration ファイル)
- **23 pages** + **29 components**、うち **~36 ファイル** に日本語直書き文字列あり
- Phase 2b で `app/src/lib/api-errors.ts:JA_MESSAGES` がすでに code-keyed 構造で存在 → Phase 3 では JSON catalog に移管

---

## 1. PR 分割案 (推奨)

Phase 3 の規模 (~2-4 週間) を考えて **8 PR に分割**することを推奨:

| # | PR | スコープ | サイズ | 期間目安 |
|---|---|---|---|---|
| PR-A | Phase 3a: 基盤導入 | next-intl install + i18n config + 空 catalog 構造 + proxy.ts ロケール検出 + Vitest missing-key + `preferred_locale` migration + locale 切替 API + **layout の 1 文字列で smoke** | 中 | 1-2 日 |
| PR-B | Phase 3b: 共通レイアウト / Header / 言語切替 UI | AdminLayout / Header / Sidebar / global error boundary / 言語切替 dropdown (地球儀アイコン) | 中 | 1-2 日 |
| PR-C | Phase 3c: Auth 系画面 | login / password-reset/{request,confirm} / accept-invitation | 中 | 1-2 日 |
| PR-D | Phase 3d: Tilesets | 一覧 / 新規 / 詳細 / 編集 + tileset-form コンポーネント | 中 | 2 日 |
| PR-E | Phase 3e: Features | 一覧 / 新規 / 詳細 / 編集 / インポート + feature-form / GeometryEditor 説明文 | 中-大 | 2-3 日 |
| PR-F | Phase 3f: Datasources / API Keys / Settings | datasources / api-keys / settings 全画面 | 中 | 2 日 |
| PR-G | Phase 3g: Teams | teams list / detail / invitation 系 | 中 | 1-2 日 |
| PR-H | Phase 3h: 仕上げ + CI strict | api-errors.ts の JA_MESSAGES を catalog に移管、eslint-plugin-i18next 試験導入、未対応文字列の最終洗い出し、ハードコード検出 | 中 | 1-2 日 |

**順序の依存性**:
- PR-A は他すべての前提 (catalog 構造と useTranslations() の動作)
- PR-B は PR-C 以降の前提 (Header の言語切替 UI が無いと「ユーザーが切り替えられない」)
- PR-C 〜 PR-G は互いに独立 (順序を入れ替え可)
- PR-H は最後

**本ドキュメントでは PR-A の Task 一覧と粒度を詳細化**する。PR-B 以降は PR-A 完了後にこの計画書を更新する形で進める (細かすぎる事前計画は実情と乖離する)。

---

## 2. PR-A (Phase 3a): 基盤導入

### 2.0 File Structure

```
app/
  package.json                        # MOD: next-intl 追加
  next.config.ts                       # MOD: createNextIntlPlugin でラップ
  src/
    i18n/
      config.ts                       # NEW: locales, defaultLocale, namespace 一覧
      request.ts                       # NEW: next-intl の getRequestConfig (Server Components 用)
      locale-cookie.ts                # NEW: cookie 名定数 + util (set / get)
    proxy.ts                          # MOD: 既存 auth decision + locale 検出 (cookie 設定)
    locales/
      en/
        common.json                   # NEW: 最小限の key (smoke 用) "app.title": "geo-base Admin"
        api-errors.json               # NEW: api-errors.ts:JA_MESSAGES を英語化したもの (Phase 2b 統合の準備)
      ja/
        common.json                   # NEW: "app.title": "geo-base 管理画面" 等
        api-errors.json               # NEW: api-errors.ts:JA_MESSAGES の中身を移行
    lib/
      api-errors.ts                   # MOD: JA_MESSAGES → useTranslations() / getTranslations() 経由に切替 (一時的に require dual path)
      auth/
        client.ts                     # MOD: switchLocale(locale) を追加、PATCH /api/auth/me/locale
    components/
      layout/
        admin-layout.tsx              # MOD: 1 か所だけ useTranslations() を使う smoke
    locales/__tests__/
      keysets.test.ts                 # NEW: vitest で en と ja のキーセット一致を assert

api/
  lib/
    routers/
      auth.py                          # MOD: PATCH /api/auth/me/locale を追加
    models/
      user.py                          # MOD (or create): preferred_locale フィールドを response model に
  lib/
    auth/
      provider.py                      # MOD: get_current_user で preferred_locale を返す

docker/
  postgis-init/
    07_preferred_locale.sql           # NEW (06_api_keys_schema.sql の次): users.preferred_locale 追加
                                       # (本番 DB には別途 migration を流す手順を docs/POSTGRES_SETUP.md に追記)
```

### 2.1 Task 1: 依存追加 + next.config 接続

**Files:**
- Modify: `app/package.json`
- Modify: `app/next.config.ts`
- Create: `app/src/i18n/config.ts`
- Create: `app/src/i18n/request.ts`

- [ ] **Step 1**: `cd app && npm install next-intl`

- [ ] **Step 2**: `app/src/i18n/config.ts`:

```typescript
// app/src/i18n/config.ts
export const LOCALES = ["en", "ja"] as const;
export type Locale = (typeof LOCALES)[number];

export const DEFAULT_LOCALE: Locale = "en";

// 既存日本語ユーザー体験を保つために、未指定時は ja を優先する
// (Cookie 未設定 + Accept-Language で ja 優先のとき)。詳細は proxy.ts。
export const FALLBACK_LOCALE_FOR_ACCEPT_LANGUAGE: Locale = "ja";

// 既知 namespace。新しい namespace を追加したら本配列にも追加する
// (missing-key テストがこの配列を反復するため)。
export const NAMESPACES = ["common", "api-errors"] as const;
export type Namespace = (typeof NAMESPACES)[number];
```

- [ ] **Step 3**: `app/src/i18n/request.ts`:

```typescript
// app/src/i18n/request.ts
import { getRequestConfig } from "next-intl/server";
import { cookies, headers } from "next/headers";

import {
  DEFAULT_LOCALE,
  LOCALES,
  Locale,
  NAMESPACES,
} from "./config";
import { LOCALE_COOKIE_NAME } from "./locale-cookie";

export default getRequestConfig(async () => {
  const cookieStore = await cookies();
  const cookieLocale = cookieStore.get(LOCALE_COOKIE_NAME)?.value;
  // proxy.ts で cookie に書いた値を尊重する。
  const locale: Locale = (LOCALES as readonly string[]).includes(
    cookieLocale ?? "",
  )
    ? (cookieLocale as Locale)
    : DEFAULT_LOCALE;

  // namespace ごとに JSON を読み込む。
  const messages: Record<string, Record<string, unknown>> = {};
  for (const ns of NAMESPACES) {
    messages[ns] = (await import(`@/locales/${locale}/${ns}.json`)).default;
  }

  return { locale, messages };
});
```

- [ ] **Step 4**: `app/src/i18n/locale-cookie.ts`:

```typescript
export const LOCALE_COOKIE_NAME = "NEXT_LOCALE";
// 1 年 (秒)
export const LOCALE_COOKIE_MAX_AGE = 60 * 60 * 24 * 365;
```

- [ ] **Step 5**: `app/next.config.ts` を `createNextIntlPlugin` でラップ:

```typescript
import createNextIntlPlugin from "next-intl/plugin";

const withNextIntl = createNextIntlPlugin("./src/i18n/request.ts");

const nextConfig = { /* 既存設定 */ };

export default withNextIntl(nextConfig);
```

- [ ] **Step 6**: `npm run build` で smoke。エラーなくビルドできることを確認。

- [ ] **Step 7**: コミット:

```
feat(app): Phase 3a — next-intl 導入と i18n 基盤 (#107)

next-intl 4.x を追加し、Server / Client Components 両対応の getRequestConfig を
配置。LOCALES / DEFAULT_LOCALE / NAMESPACES を i18n/config.ts に集約。
本 commit では catalog 内容は最小限 (Task 2 で内容を追加)。

Refs: #107 #109
```

### 2.2 Task 2: 空 catalog + missing-key Vitest

**Files:**
- Create: `app/src/locales/en/common.json`
- Create: `app/src/locales/ja/common.json`
- Create: `app/src/locales/en/api-errors.json`
- Create: `app/src/locales/ja/api-errors.json`
- Create: `app/src/locales/__tests__/keysets.test.ts`

- [ ] **Step 1**: `app/src/locales/en/common.json` (smoke 用 1 key):

```json
{
  "app": {
    "title": "geo-base Admin"
  }
}
```

- [ ] **Step 2**: `app/src/locales/ja/common.json`:

```json
{
  "app": {
    "title": "geo-base 管理画面"
  }
}
```

- [ ] **Step 3**: `app/src/locales/{en,ja}/api-errors.json` — Phase 2b の `api-errors.ts:JA_MESSAGES` をベースに 2 言語版を作る:
  - `ja/api-errors.json`: 現行の `JA_MESSAGES` をそのまま JSON 化
  - `en/api-errors.json`: 同じキーで英語訳 (例: `auth_invalid_credentials: "Invalid email or password"` 等、`api/lib/errors.py` の `api_error()` で使われている message と揃える)

- [ ] **Step 4**: `app/src/locales/__tests__/keysets.test.ts`:

```typescript
import { describe, it, expect } from "vitest";

import { NAMESPACES, LOCALES } from "@/i18n/config";

/**
 * 全 namespace で en と ja の key set が完全一致することを assert。
 * 翻訳忘れ / 余分なキーを CI で検出する。
 */
function flatKeys(obj: unknown, prefix = ""): string[] {
  if (!obj || typeof obj !== "object") return [];
  return Object.entries(obj as Record<string, unknown>).flatMap(([k, v]) => {
    const key = prefix ? `${prefix}.${k}` : k;
    if (v && typeof v === "object" && !Array.isArray(v)) {
      return flatKeys(v, key);
    }
    return [key];
  });
}

describe("locale keysets", () => {
  for (const ns of NAMESPACES) {
    it(`${ns}: en と ja の keys が一致`, async () => {
      const en = (await import(`@/locales/en/${ns}.json`)).default;
      const ja = (await import(`@/locales/ja/${ns}.json`)).default;
      const enKeys = flatKeys(en).sort();
      const jaKeys = flatKeys(ja).sort();
      expect(jaKeys).toEqual(enKeys);
    });
  }
});
```

- [ ] **Step 5**: `npm test -- --run src/locales/__tests__/keysets.test.ts` で PASS 確認。

- [ ] **Step 6**: コミット:

```
test(app): Phase 3a — 空 catalog + missing-key 検知テスト (#107)
```

### 2.3 Task 3: proxy.ts に locale 検出

**Files:**
- Modify: `app/src/proxy.ts`
- Modify: `app/src/lib/auth/proxy-decisions.ts` (該当する場合のみ)

- [ ] **Step 1**: 既存 `proxy.ts` の `decideProxy()` 戻り後に locale 解決ロジックを追加:

```typescript
import { LOCALES, DEFAULT_LOCALE, FALLBACK_LOCALE_FOR_ACCEPT_LANGUAGE } from "@/i18n/config";
import { LOCALE_COOKIE_NAME, LOCALE_COOKIE_MAX_AGE } from "@/i18n/locale-cookie";

function resolveLocale(request: NextRequest): string {
  // 1) 既存 cookie 優先
  const existing = request.cookies.get(LOCALE_COOKIE_NAME)?.value;
  if (existing && (LOCALES as readonly string[]).includes(existing)) {
    return existing;
  }

  // 2) Accept-Language の最初の locale が LOCALES に含まれれば使う
  const acceptLanguage = request.headers.get("accept-language") ?? "";
  const candidates = acceptLanguage
    .split(",")
    .map((part) => part.split(";")[0].trim().split("-")[0].toLowerCase());
  for (const c of candidates) {
    if ((LOCALES as readonly string[]).includes(c)) {
      // 既存日本語ユーザー保護: cookie 未設定で Accept-Language に ja があれば ja
      return c;
    }
  }

  // 3) fallback (戦略 spec の「既存日本語ユーザー保護」のため一旦 ja)
  return FALLBACK_LOCALE_FOR_ACCEPT_LANGUAGE;
}

// 既存 proxy() の戻り直前で cookie をセット
export function proxy(request: NextRequest) {
  // ... 既存処理で response を作る
  const response = ... ; // 既存 logic

  // locale 未設定なら resolveLocale() の結果を cookie に書く
  if (!request.cookies.get(LOCALE_COOKIE_NAME)) {
    response.cookies.set(LOCALE_COOKIE_NAME, resolveLocale(request), {
      maxAge: LOCALE_COOKIE_MAX_AGE,
      path: "/",
      sameSite: "lax",
    });
  }
  return response;
}
```

- [ ] **Step 2**: 既存の `proxy-decisions.test.ts` が壊れていないことを確認 (`npm test`)。

- [ ] **Step 3**: コミット。

### 2.4 Task 4: layout で smoke (1 文字列だけ翻訳経路を使う)

**Files:**
- Modify: `app/src/app/layout.tsx` or `app/src/components/layout/admin-layout.tsx`

- [ ] **Step 1**: トップレベル layout に `NextIntlClientProvider` を入れる:

```tsx
// app/src/app/layout.tsx (例)
import { NextIntlClientProvider } from "next-intl";
import { getLocale, getMessages } from "next-intl/server";

export default async function RootLayout({ children }: { children: React.ReactNode }) {
  const locale = await getLocale();
  const messages = await getMessages();
  return (
    <html lang={locale}>
      <body>
        <NextIntlClientProvider locale={locale} messages={messages}>
          {children}
        </NextIntlClientProvider>
      </body>
    </html>
  );
}
```

- [ ] **Step 2**: AdminLayout の Header title を `useTranslations("common")("app.title")` 経由に書き換える (smoke)。

- [ ] **Step 3**: dev server を起動して、cookie に `NEXT_LOCALE=ja` を入れたら「geo-base 管理画面」、`en` なら「geo-base Admin」が表示されることを確認 (実際に server を起動する手間が大きい場合は省略可、build 通過で OK)。

- [ ] **Step 4**: コミット。

### 2.5 Task 5: `preferred_locale` 永続化 (DB + API + Auth client)

**Files:**
- Create: `docker/postgis-init/07_preferred_locale.sql`
- Modify: `api/lib/routers/auth.py`
- Modify: `api/lib/models/user.py` (or 該当する response model)
- Modify: `api/lib/auth/provider.py` (or 該当する user fetch path)
- Modify: `app/src/lib/auth/client.ts`

- [ ] **Step 1**: migration:

```sql
-- docker/postgis-init/07_preferred_locale.sql
ALTER TABLE users
  ADD COLUMN IF NOT EXISTS preferred_locale VARCHAR(8);

COMMENT ON COLUMN users.preferred_locale IS
  'i18n Phase 3: ユーザーが明示的に切替した locale (例: ja, en)。NULL の場合は cookie / Accept-Language にフォールバック。';
```

- [ ] **Step 2**: `users` のレスポンスモデルに `preferred_locale: Optional[str]` 追加。

- [ ] **Step 3**: API endpoint:

```python
# api/lib/routers/auth.py
class LocalePatchModel(BaseModel):
    preferred_locale: Optional[str] = Field(None, max_length=8)

@router.patch("/me/locale", response_model=UserResponse)
async def update_my_locale(
    body: LocalePatchModel,
    user: User = Depends(require_auth),
):
    """ログイン中ユーザーの preferred_locale を更新する。"""
    allowed = {"en", "ja", None}  # 初期は 2 言語、null で「明示切替なし」へ戻す
    if body.preferred_locale not in allowed:
        raise api_error(
            400,
            ErrorCode.VALIDATION_INVALID_VALUE,
            "Unsupported locale",
            details={"preferred_locale": body.preferred_locale},
        )
    with get_db_connection() as conn:
        ...UPDATE users SET preferred_locale = %s WHERE id = %s RETURNING *
    return user_response
```

- [ ] **Step 4**: `app/src/lib/auth/client.ts` に `setPreferredLocale(locale: "en" | "ja" | null)` 追加。`PATCH /api/auth/me/locale` を呼ぶ + cookie 更新。

- [ ] **Step 5**: pytest 追加 (api/tests/test_auth_locale.py): success / invalid / unauthenticated の 3 ケース。

- [ ] **Step 6**: `cd app && npx tsc --noEmit` + vitest + `cd api && pytest` で全 green。

- [ ] **Step 7**: コミット。

### 2.6 Task 6: PR-A 検証 + draft PR

- [ ] `python3 .github/workflows/scripts/i18n_guard.py` exit 0
- [ ] `cd app && npx tsc --noEmit` clean
- [ ] `cd app && npm test` 全件 PASS (vitest)
- [ ] `cd app && npm run build` ビルド通過
- [ ] `cd api && uv run pytest tests/test_auth_locale.py -q` PASS
- [ ] E2E smoke (PR push 後) を観察
- [ ] PR-A を draft PR 化

---

## 3. PR-B (Phase 3b): Header に言語切替 UI + 共通 layout の catalog 化

**Status**: implemented (本 PR)。検証結果は 3.5 を参照。

PR-A で基盤 (`useTranslations()` の経路、`setPreferredLocale()` API、`NEXT_LOCALE` cookie) が揃っている。PR-B では「ユーザーが UI から言語を切り替えられる」状態にし、同時に **`common` namespace** を本格運用に乗せる。

### 3.0 File Structure (差分)

```
app/
  src/
    components/
      layout/
        admin-layout.tsx              # MOD: 既存 sm:inline の email や Sidebar 周辺の文言を `useTranslations()` 化
        sidebar.tsx                   # MOD: ナビゲーション ("ダッシュボード"/"タイルセット"/...) を catalog 化
        language-switcher.tsx         # NEW: 地球儀 dropdown (Client Component)
    locales/
      en/common.json                  # MOD: nav.* / language.* / user.* を追加
      ja/common.json                  # MOD: 同じ key で日本語
    hooks/
      use-locale-switcher.ts          # NEW: `authClient.setPreferredLocale()` + cookie + router.refresh() を 1 関数で

app/tests/e2e/
  i18n/
    language-switcher.spec.ts        # NEW: 切替後の即時反映 + DB 永続化 + reload 後維持を E2E で確認
```

### 3.1 Task 1: `common` namespace を拡充 (Sidebar 文言 + nav)

**Files:**
- Modify: `app/src/locales/en/common.json`
- Modify: `app/src/locales/ja/common.json`

- [ ] **Step 1**: 既存 `Sidebar` の `navItems` を抽出して catalog 化:

```json
// app/src/locales/ja/common.json (差分)
{
  "app": { "title": "geo-base 管理画面" },
  "nav": {
    "dashboard": "ダッシュボード",
    "tilesets": "タイルセット",
    "features": "フィーチャー",
    "datasources": "データソース",
    "teams": "チーム",
    "apiKeys": "API キー",
    "settings": "設定"
  },
  "user": {
    "logout": "ログアウト"
  },
  "language": {
    "switch": "言語を切り替える",
    "label_en": "English",
    "label_ja": "日本語"
  }
}
```

英語版も同じ key で `"Dashboard"`, `"Tilesets"`, ... を入れる。

- [ ] **Step 2**: `app/src/locales/__tests__/keysets.test.ts` で同期確認 (既存 PR-A の Vitest が自動でカバー、新規 test 追加不要)。

### 3.2 Task 2: `LanguageSwitcher` Client Component

**Files:**
- Create: `app/src/components/layout/language-switcher.tsx`
- Create: `app/src/hooks/use-locale-switcher.ts`

- [ ] **Step 1**: hook 実装

```tsx
// app/src/hooks/use-locale-switcher.ts
"use client";
import { useRouter } from "next/navigation";
import { useTransition } from "react";
import { authClient } from "@/lib/auth/client";
import { LOCALE_COOKIE_NAME, LOCALE_COOKIE_MAX_AGE } from "@/i18n/locale-cookie";
import type { Locale } from "@/i18n/config";

export function useLocaleSwitcher() {
  const router = useRouter();
  const [isPending, startTransition] = useTransition();

  const switchTo = (locale: Locale) => {
    startTransition(async () => {
      // 1. cookie を先に書く (next-intl/request.ts が次の SSR で読む)
      document.cookie = `${LOCALE_COOKIE_NAME}=${locale}; max-age=${LOCALE_COOKIE_MAX_AGE}; path=/; samesite=lax`;
      // 2. DB に永続化 (ログイン中のみ。失敗しても cookie だけで動作するので silent)
      try {
        await authClient.setPreferredLocale(locale);
      } catch (err) {
        console.warn("setPreferredLocale failed (cookie still saved):", err);
      }
      // 3. RSC を再評価して翻訳を反映
      router.refresh();
    });
  };

  return { switchTo, isPending };
}
```

- [ ] **Step 2**: dropdown component

```tsx
// app/src/components/layout/language-switcher.tsx
"use client";
import { Globe } from "lucide-react";
import { useLocale, useTranslations } from "next-intl";
import {
  DropdownMenu,
  DropdownMenuContent,
  DropdownMenuItem,
  DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { Button } from "@/components/ui/button";
import { useLocaleSwitcher } from "@/hooks/use-locale-switcher";
import type { Locale } from "@/i18n/config";

export function LanguageSwitcher() {
  const t = useTranslations("common.language");
  const current = useLocale() as Locale;
  const { switchTo, isPending } = useLocaleSwitcher();

  return (
    <DropdownMenu>
      <DropdownMenuTrigger asChild>
        <Button
          variant="ghost"
          size="sm"
          aria-label={t("switch")}
          disabled={isPending}
          data-testid="language-switcher"
        >
          <Globe className="h-4 w-4" />
          <span className="ml-2 text-xs uppercase">{current}</span>
        </Button>
      </DropdownMenuTrigger>
      <DropdownMenuContent align="end">
        <DropdownMenuItem
          onClick={() => switchTo("en")}
          data-testid="language-switcher-en"
          aria-current={current === "en"}
        >
          {t("label_en")}
        </DropdownMenuItem>
        <DropdownMenuItem
          onClick={() => switchTo("ja")}
          data-testid="language-switcher-ja"
          aria-current={current === "ja"}
        >
          {t("label_ja")}
        </DropdownMenuItem>
      </DropdownMenuContent>
    </DropdownMenu>
  );
}
```

### 3.3 Task 3: AdminLayout / Sidebar の catalog 化

**Files:**
- Modify: `app/src/components/layout/admin-layout.tsx`
- Modify: `app/src/components/layout/sidebar.tsx`

- [ ] **Step 1**: AdminLayout Header に `<LanguageSwitcher />` を user info の左に挿入。

- [ ] **Step 2**: Sidebar の `navItems` の `title` を `useTranslations("common.nav")(...)` で動的に置き換え。`navItems` 定義を `[{ key: "dashboard", href: "/", icon: Home }, ...]` の形に変えて、render 時に `t(item.key)` する。

- [ ] **Step 3**: Sidebar の "geo-base" ロゴ + "geo-base Admin v0.2.0" footer を `t("app.title")` 経由に。

### 3.4 Task 4: E2E spec

**Files:**
- Create: `app/tests/e2e/i18n/language-switcher.spec.ts`

- [ ] **Step 1**: テストパターン:
  1. デフォルト (ja cookie で開く) で Sidebar の `ダッシュボード` 表示確認
  2. `language-switcher` クリック → `language-switcher-en` クリック
  3. `Dashboard` に切り替わる (= router.refresh() の効果)
  4. page reload → `Dashboard` がまだ表示される (= cookie + DB 永続化)
  5. `language-switcher-ja` で戻す
  6. `expect(document.cookie)` で `NEXT_LOCALE=en` → `NEXT_LOCALE=ja` の遷移を検証

- [ ] **Step 2**: spec を smoke 対象外 (`@smoke` タグなし) にして、full E2E と nightly のみで走るようにする。

### 3.5 Task 5: 検証 + draft PR

- [x] tsc / vitest (keysets で nav/language/user キーが en/ja で一致 — 4 files / 77 tests green) / `npm run build` 通過
- [x] playwright `--list` で `language-switcher.spec.ts` が認識される
- [x] i18n-guard exit 0 (新規 catalog に問題なし)
- [x] PR-B を push (実 E2E 走行は CI で確認)

### 3.6 リスク

| リスク | 緩和 |
|---|---|
| ヘッダーが narrow viewport で LanguageSwitcher + email が collapse | `sm:inline` で email を隠す既存の挙動を維持、language-switcher は常時表示 |
| Server Components の翻訳が router.refresh() で反映されない | next-intl の `getRequestConfig` は毎リクエストで cookie を読むので、refresh() で再 fetch される (動作確認は E2E で) |
| 切替直後の race (cookie set 前に refresh が走る) | hook 内の順序を「cookie → API → refresh」固定、startTransition で 1 まとめにする |
| 既存 Sidebar の `navItems` 配列の `title` が他箇所でも使われている | grep で参照箇所を確認し、すべての利用を `t(item.key)` に統一 |

---

## 4. PR-C (Phase 3c): Auth 画面の catalog 化

**Status**: planned (PR-B merge 後に着手)。

PR-A で `next-intl` 基盤、PR-B で Header / Sidebar の catalog 化が完了。PR-C は **AdminLayout 外** で動く Auth 系画面 (login / password-reset/{request,confirm} / accept-invitation) を catalog 化する。これら 4 画面は既存 testid を維持できるため E2E 影響は最小、ただし新 namespace `auth` を導入する。

### 4.0 対象画面 + ハードコード文字列の事前調査結果

| 画面 | ファイル | ハードコード JP |
|---|---|---|
| Login | `app/src/app/login/page.tsx` | `ログイン` (h1), `メールアドレス` (placeholder), `パスワード` (placeholder), `ログイン` (button), `パスワードをお忘れですか？` (link) |
| Password reset request | `app/src/app/password-reset/request/page.tsx` | `確認` (success h1), `該当する email …送信しました。` (success p), `パスワードリセット` (h1), `メールアドレス` (placeholder), `送信` (button) |
| Password reset confirm | `app/src/app/password-reset/confirm/page.tsx` | `無効なリンクです。` (error), `パスワードを更新しました。` (success), `ログインへ` (button), `新しいパスワードを設定` (h1), `新しいパスワード（8文字以上）` (placeholder), `更新` (button) |
| Accept invitation | `app/src/app/accept-invitation/page.tsx` | `Loading...` (既に英語), `チーム招待: <team>` (h1 ×2), `この email には既にアカウントがあります。…` (p), `ログイン` (link button), `役割: <role>` (p), `お名前` (placeholder), `パスワード（8文字以上）` (placeholder), `アカウント作成して参加` (button) |

ロード中の `Loading...` と error 文言 (`Invalid invitation link` / `Failed to accept invitation`) は既に英語のため、JA 用 messages も英語維持 OR 日本語化のどちらかを catalog で選択する。Phase 3c では **画面文言の翻訳完全化** を優先し、`Loading...` も catalog 化して両 locale で揃える。

### 4.1 File Structure (差分)

```
app/
  src/
    i18n/
      config.ts                       # MOD: NAMESPACES に "auth" を追加
    locales/
      en/auth.json                    # NEW: login / reset / invitation の英語
      ja/auth.json                    # NEW: 同じキーで日本語
    app/
      login/page.tsx                  # MOD: useTranslations("auth.login")
      password-reset/request/page.tsx # MOD: useTranslations("auth.passwordReset.request")
      password-reset/confirm/page.tsx # MOD: useTranslations("auth.passwordReset.confirm")
      accept-invitation/page.tsx      # MOD: useTranslations("auth.invitation")
```

### 4.2 Task 1: `auth` namespace を i18n config に追加

**Files:**
- Modify: `app/src/i18n/config.ts`

- [ ] **Step 1**: `NAMESPACES` 配列に `"auth"` を追加 (`["common", "api-errors", "auth"] as const`)。
- [ ] **Step 2**: `keysets.test.ts` は配列を反復するため自動で `auth` namespace もチェックされる (新規 test 不要)。
- [ ] **Step 3**: コミット (test は次タスクで catalog 追加後にまとめて run)。

### 4.3 Task 2: `auth.json` catalog を新規作成

**Files:**
- Create: `app/src/locales/en/auth.json`
- Create: `app/src/locales/ja/auth.json`

提案するキー構造 (`<page>.<section>.<element>` の 3 階層):

```json
{
  "login": {
    "title": "Sign in",
    "email_placeholder": "Email",
    "password_placeholder": "Password",
    "submit": "Sign in",
    "submitting": "...",
    "forgot_password": "Forgot your password?"
  },
  "passwordReset": {
    "request": {
      "title": "Reset password",
      "email_placeholder": "Email",
      "submit": "Send",
      "success_title": "Confirm",
      "success_message": "If the email is registered, we have sent instructions to reset your password."
    },
    "confirm": {
      "title": "Set a new password",
      "password_placeholder": "New password (8+ chars)",
      "submit": "Update",
      "submitting": "...",
      "invalid_link": "This link is invalid.",
      "success_message": "Password updated.",
      "back_to_login": "Go to sign in"
    }
  },
  "invitation": {
    "loading": "Loading...",
    "title": "Team invitation: {team}",
    "role": "Role: {role}",
    "existing_account_message": "An account already exists for this email. Please sign in first to accept.",
    "existing_account_login": "Sign in",
    "name_placeholder": "Your name",
    "password_placeholder": "Password (8+ chars)",
    "submit": "Create account and join",
    "submitting": "...",
    "error_invalid_link": "Invalid invitation link",
    "error_not_found": "Invitation not found",
    "error_generic": "Failed to accept invitation"
  }
}
```

JA 版は同一構造、文字列は現行ハードコードを正本にする。ICU プレースホルダ `{team}` / `{role}` は next-intl の標準フォーマット (Phase 2b で導入済みのため別途設定不要)。

- [ ] **Step 1**: `en/auth.json` を上記内容で作成。
- [ ] **Step 2**: `ja/auth.json` を同じ key set で作成。`title: "ログイン"` / `email_placeholder: "メールアドレス"` 等、現状文言をそのまま入れる。
- [ ] **Step 3**: `npm test -- --run src/locales/__tests__/keysets.test.ts` で 3 namespace (`common` / `api-errors` / `auth`) すべてが PASS することを確認。
- [ ] **Step 4**: コミット。

### 4.4 Task 3: login/page.tsx を catalog 化

**Files:**
- Modify: `app/src/app/login/page.tsx`

- [ ] **Step 1**: `import { useTranslations } from "next-intl";` を追加。
- [ ] **Step 2**: `LoginForm` の冒頭で `const t = useTranslations("auth.login");`。
- [ ] **Step 3**: h1 / placeholder / submit / forgot-password link を `t("title")` / `t("email_placeholder")` / 等に置換。
- [ ] **Step 4**: submit ボタンの `loading ? "..." : "ログイン"` を `loading ? t("submitting") : t("submit")` に。
- [ ] **Step 5**: dev server (`npm run dev`) で `?lang=ja` cookie 設定状態と `lang=en` 状態の両方で表示確認。
- [ ] **Step 6**: 既存 E2E (`auth/login.spec.ts` 等) が testid ベースで動くため壊れないことを `--list` で確認後、可能なら 1 spec だけ実走。

### 4.5 Task 4: password-reset/{request,confirm} を catalog 化

**Files:**
- Modify: `app/src/app/password-reset/request/page.tsx`
- Modify: `app/src/app/password-reset/confirm/page.tsx`

- [ ] **Step 1**: request 側で `const t = useTranslations("auth.passwordReset.request");`、success 画面と form 画面の文字列を置換。
- [ ] **Step 2**: confirm 側で `const t = useTranslations("auth.passwordReset.confirm");`、無効リンク / 成功画面 / form の文字列を置換。
- [ ] **Step 3**: `AUTH-08` E2E (`password-reset-confirm-success` testid) は触らない (selector 互換性維持)。

### 4.6 Task 5: accept-invitation/page.tsx を catalog 化 (ICU プレースホルダ含む)

**Files:**
- Modify: `app/src/app/accept-invitation/page.tsx`

- [ ] **Step 1**: `const t = useTranslations("auth.invitation");`。
- [ ] **Step 2**: h1 の `チーム招待: ${info.team_name}` を `t("title", { team: info.team_name })` に。ICU プレースホルダ動作を確認 (next-intl は ICU MessageFormat ネイティブサポート)。
- [ ] **Step 3**: 既存 testid (`invitation-error` / `invitation-login-link` / `invitation-name` / `invitation-password` / `invitation-submit`) は変更しない。
- [ ] **Step 4**: error メッセージ 3 種 (`Invalid invitation link` / `Invitation not found` / `Failed to accept invitation`) は **AuthApiError 経由でない fallback 文字列**。それぞれ `t("error_invalid_link")` / `t("error_not_found")` / `t("error_generic")` に置換。

### 4.7 Task 6: 検証 + PR 作成

- [ ] `npx tsc --noEmit` clean
- [ ] `npm test -- --run` 全 vitest PASS (keysets で 3 namespace × 2 locale が同期)
- [ ] `npm run build` 通過
- [ ] `python3 .github/workflows/scripts/i18n_guard.py` exit 0
- [ ] `npx playwright test --list tests/e2e/auth/` で既存 spec が認識
- [ ] dev server で en / ja 両 locale の手動 smoke (Login 画面で switcher が動くこと)
- [ ] PR-C を `feat/issue-107-i18n-phase3c` ブランチで開く

### 4.8 リスク

| リスク | 緩和 |
|---|---|
| `info.team_name` 等 ICU プレースホルダの渡し漏れ | tsc が型エラーで検出 (next-intl v4 の TypedTranslations は v5 だが、placeholder 不一致は実行時に `MISSING_VAR` ログで気付ける)。catalog 設計時に `{team}` の利用箇所をコメントしておく |
| Login fallback error "Login failed" は **API レスポンス無しの time-out** 用なので catalog 化対象だが、`AuthApiError` のメッセージ表示は Phase 2b で API 側に責任分担済み (こちらは触らない) | catalog 化するのは `setError("Login failed")` の fallback だけ。`err.detail` の表示は変更しない |
| E2E が `getByText("ログイン")` 等で hard-code されている | grep で確認: 現状 `auth/login.spec.ts` は testid 中心。仮に文字列ベースの assertion が残っていたら spec も catalog に合わせて更新 |
| `Loading...` を catalog 化すると Suspense fallback の遅延が増える (RSC 経由の翻訳ロード) | Suspense fallback は早期描画なので、`Loading...` だけ catalog 外のリテラルに留める判断もあり (実装時に dev server で計測して決定) |

---

## 5. PR-D (Phase 3d): Tilesets 全画面の catalog 化

**Status**: implemented (PR #132、in review)。

### 5.0 対象 + 規模

| ファイル | LOC | sub-namespace |
|---|---|---|
| `app/src/app/tilesets/page.tsx` | 494 | `tilesets.list` |
| `app/src/app/tilesets/new/page.tsx` | 62 | `tilesets.new` |
| `app/src/app/tilesets/[id]/page.tsx` | 769 | `tilesets.detail` |
| `app/src/app/tilesets/[id]/edit/page.tsx` | 125 | `tilesets.edit` |
| `app/src/components/tilesets/tileset-form.tsx` | 313 | `tilesets.form` |
| `app/src/components/tilesets/delete-tileset-dialog.tsx` | 89 | `tilesets.deleteDialog` |
| **合計** | **~1850** | |

### 5.1 主要決定事項 (PR レビューを通して固まった)

- **ICU `{count}` 多用**: list の selected_count / count_badge / bulk_delete_* で利用
- **edit subtitle**: 旧 `<strong>{name}</strong>` を catalog のプレーン `{name}` に置換 (rich text 化は将来必要なら)
- **type/format Select labels**: form 側に `type_vector` 等、list 側に `filter_type_vector` 等を別途用意 (用途が違うため重複 OK)
- **date locale**: `useLocale()` → BCP47 タグ正規化 (`ja-JP` / `en-US`) で `toLocaleString` に渡す。`"ja-JP"` ハードコードは禁止 (PR #132 round 1 で確定)

---

## 6. PR-E (Phase 3e): Features 全画面の catalog 化

**Status**: planned (PR-D merge 後に着手)。

PR-D の規模 (~1850 LOC) に対し、Features は **約 3700 LOC** と倍。PR を 2 つに分割するか検討するが、本計画では一旦 **単一 PR で実施**して、レビュー負荷が大きい場合は次回以降に分割を検討する。

### 6.0 対象 + 規模

| ファイル | LOC | sub-namespace |
|---|---|---|
| `app/src/app/features/page.tsx` | 1010 | `features.list` |
| `app/src/app/features/new/page.tsx` | 129 | `features.new` |
| `app/src/app/features/[id]/page.tsx` | 463 | `features.detail` |
| `app/src/app/features/[id]/edit/page.tsx` | 188 | `features.edit` |
| `app/src/app/features/import/page.tsx` | 517 | `features.import` |
| `app/src/components/features/feature-form.tsx` | 683 | `features.form` (GeometryEditor 含む) |
| `app/src/components/features/delete-feature-dialog.tsx` | 89 | `features.deleteDialog` |
| `app/src/components/features/export-features-button.tsx` | 154 | `features.exportButton` |
| `app/src/components/features/geojson-dropzone.tsx` | 289 | `features.geojsonDropzone` |
| `app/src/components/features/geojson-preview.tsx` | 192 | `features.geojsonPreview` |
| **合計** | **~3714** | |

### 6.1 File Structure (差分)

```
app/
  src/
    i18n/config.ts                    # MOD: NAMESPACES に "features" を追加
    locales/
      en/features.json                # NEW: 10 sub-namespace
      ja/features.json                # NEW: 同上
    app/features/
      page.tsx                        # MOD: useTranslations("features.list")
      new/page.tsx                    # MOD: features.new
      [id]/page.tsx                   # MOD: features.detail
      [id]/edit/page.tsx              # MOD: features.edit
      import/page.tsx                 # MOD: features.import
    components/features/
      feature-form.tsx                # MOD: features.form (geometry editor の説明文も)
      delete-feature-dialog.tsx       # MOD: features.deleteDialog
      export-features-button.tsx      # MOD: features.exportButton
      geojson-dropzone.tsx            # MOD: features.geojsonDropzone
      geojson-preview.tsx             # MOD: features.geojsonPreview
```

### 6.2 Task 1: `features` namespace を i18n config に追加

`tilesets` のときと同様、`NAMESPACES` 配列に `"features"` を追加し、keysets.test.ts が自動で en/ja 同期を検証する。

### 6.3 Task 2: `features.json` catalog を新規作成

PR-D の経験から、初回コミットで全 sub-namespace のキーを一括で作る方が後の reviewer 確認が楽 (= "key 追加→使用箇所" の差分が混じらない)。

主要 ICU プレースホルダ候補:
- `list.selected_count`: `{count}件を選択中`
- `list.count_badge`: `{count} 件`
- `list.bulk_delete_description`: `選択した {count} 件のフィーチャーを削除します。...`
- `import.preview_count`: `{count} フィーチャー検出`
- `import.success_message`: `{count} フィーチャーをインポートしました`
- `detail.title`: `フィーチャー #{id}` 形式 (id は短縮表示)
- `deleteDialog.description`: `{name} を削除します。...` (name は properties.name or id)

### 6.4 Task 3: 一覧 + 新規 + import (list / new / import)

`app/src/app/features/{page,new/page,import/page}.tsx` を catalog 化。最も大きい list.tsx (1010 行) では tileset filter / layer filter / 一括操作 / table column / 各 toast メッセージなど多岐にわたるため、抽出キー数が多い (推定 60-80 keys)。

### 6.5 Task 4: 詳細 + 編集 (detail / edit)

`[id]/page.tsx` と `[id]/edit/page.tsx` を catalog 化。detail はメタデータ表示と GeoJSON プレビューが主。edit は feature-form 経由なので画面側の文言は少ない。

### 6.6 Task 5: コンポーネント群 (form / dialog / export / dropzone / preview)

最大の `feature-form.tsx` (683 行) は GeometryEditor を含み、座標入力ガイダンス、ジオメトリタイプ Select、properties エディタなど多くの labels / help text を持つ。`geojson-dropzone` と `geojson-preview` は import flow の UI で、エラー検証メッセージや preview 表示文言を持つ。

### 6.7 Task 6: date locale + 検証 + PR 作成

- date 表示は `useLocale()` ベースの BCP47 タグ (`ja-JP` / `en-US`) を `toLocaleString` に渡す形に統一 (PR-D で確立したパターン)
- `npx tsc --noEmit` clean / `npm test` 全 vitest PASS (5 namespace × en/ja keysets が確認される) / `npm run build` 通過 / `python3 .github/workflows/scripts/i18n_guard.py` exit 0
- E2E `--list` で features 系 spec が認識される (testid 維持により破壊なし想定)
- PR を `feat/issue-107-i18n-phase3e` ブランチで開く

### 6.8 リスク

| リスク | 緩和 |
|---|---|
| PR が大きすぎてレビューしづらい (~3700 LOC) | sub-namespace ごとに commit 分割 (catalog 整備 / 各画面 / 各コンポーネント)。レビュー時に commit 順で読める形にする |
| GeometryEditor の座標入力ガイダンス文言で技術用語と日常語が混在 | `form.help_*` keys に集約し、英語版は技術文書寄り (e.g. "Comma-separated coordinates")、日本語版は丁寧文 (e.g. 「カンマ区切りで...」) で書き分ける |
| import flow の error message が大量 (GeoJSON 検証エラー) | 既存の error 構造を保ちつつ catalog 化。エラーコード化は本 PR スコープ外 (PR-H の API-errors 統合に近い性格) |
| feature-form の `properties` (任意 key=value) の placeholder | property key / value 入力欄の placeholder は技術的なため英語固定で良い (catalog 化スコープ外と判断する場合あり) |
| date locale を式中ですぐ使うため `useLocale` の依存が増える | PR-D 同様、page top で 1 度 `dateLocale` を計算して関数内で使い回す形に統一 |

---

## 7. PR-F (Phase 3f): Datasources / API Keys / Settings 画面の catalog 化

**Status**: planned (PR-E merge 後に着手)。

3 ドメインを 1 PR に束ねる構成。各画面は独立だが、いずれも認証済みユーザーの「自分の設定」系統で、E2E の影響範囲も似ているため。

### 7.0 対象 + 規模

| ファイル | LOC | sub-namespace |
|---|---|---|
| `app/src/app/datasources/page.tsx` | 659 | `datasources.list` |
| `app/src/app/datasources/new/page.tsx` | 580 | `datasources.new` |
| `app/src/app/datasources/[id]/page.tsx` | 534 | `datasources.detail` |
| `app/src/app/api-keys/page.tsx` | 650 | `apiKeys.list` (page 内に dialog 同居) |
| `app/src/app/settings/page.tsx` | 5 | (redirect のみ、catalog 化対象外) |
| `app/src/app/settings/profile/page.tsx` | 155 | `settings.profile` |
| `app/src/app/settings/password/page.tsx` | 123 | `settings.password` |
| `app/src/components/settings/settings-nav.tsx` | 39 | `settings.nav` |
| **合計** | **~2745** | |

### 7.1 File Structure (差分)

```
app/
  src/
    i18n/config.ts                    # MOD: NAMESPACES に "datasources" / "apiKeys" / "settings" を追加
    locales/
      en/datasources.json             # NEW: list / new / detail
      en/apiKeys.json                 # NEW: list (+ create/revoke/delete dialog 群)
      en/settings.json                # NEW: profile / password / nav
      ja/datasources.json             # NEW
      ja/apiKeys.json                 # NEW
      ja/settings.json                # NEW
    app/datasources/{page,new/page,[id]/page}.tsx  # MOD
    app/api-keys/page.tsx                          # MOD
    app/settings/{profile/page,password/page}.tsx  # MOD
    components/settings/settings-nav.tsx           # MOD
```

### 7.2 Task 1: 3 namespace 追加 + 空 catalog 作成

`NAMESPACES` に `datasources`, `apiKeys`, `settings` を追加し、keyset 同期テストを通すための空 / 最小 catalog をまず作る。実画面の文字列抽出は Task 2-4 で各画面ごとに行う。

### 7.3 Task 2: Datasources 3 画面 (`list` / `new` / `detail`)

- `list` (659 LOC): 一覧 / フィルタ (タイプ / 公開状態) / 一括削除 / table columns
- `new` (580 LOC): タイプ別フォーム (URL 入力, S3 設定, ローカル PMTiles アップロード等)
- `detail` (534 LOC): メタデータ表示、プレビュー、編集 / 削除アクション

ICU プレースホルダ候補:
- `list.selected_count`: `{count}件を選択中`
- `list.bulk_delete_description`: `選択した {count} 件のデータソース...`
- `detail.subtitle_template`: `データソース "{name}"` 等
- `new.upload_progress`: `{filename} をアップロード中 ({percent}%)` (PMTiles upload 専用)

### 7.4 Task 3: API Keys (`list` + 各種 dialog)

`api-keys/page.tsx` は 650 LOC で、ページ内に複数 dialog を持つ:
- 作成 dialog (name + scopes + expires_in_days 入力 + plaintext key 表示)
- 失効 dialog (理由入力 optional)
- 削除 dialog (確認)
- 失効済みキー含めるトグル

ICU プレースホルダ:
- `list.plaintext_warning`: `この API キーは二度と表示されません` (固定文)
- `revoke_dialog.description_template`: `"{name}" を失効させます...`

masked key (`gb_live_********`) は技術文字列なので catalog 化スコープ外と判断。

### 7.5 Task 4: Settings (`profile` / `password` / `nav`)

- `profile/page.tsx` (155 LOC): 名前・メール編集フォーム、`humanizeDetail` の "対象ユーザーが見つかりません..." 文言含む
- `password/page.tsx` (123 LOC): 現在パス + 新パス + 確認の 3 フィールド + 強度要件メッセージ
- `settings-nav.tsx` (39 LOC): プロファイル / パスワード のタブラベル

NAV は既に Phase 3b で `common.nav.settings` を持っているが、settings 内タブは domain 別の `settings.nav.{profile,password}` を新規追加。

### 7.6 Task 5: date locale + 検証 + PR 作成

PR-D / PR-E と同じく `useLocale()` ベースの BCP47 タグ正規化を採用 (api-keys の last_used_at, created_at, expires_at 表示など)。

検証: tsc / vitest (6 namespace × en/ja keysets PASS) / build / i18n-guard / playwright list。

### 7.7 リスク

| リスク | 緩和 |
|---|---|
| api-keys page が dialog を内包するため 1 ファイルが肥大化 | 必要に応じて catalog 化と同時に dialog を別コンポーネントに分割する判断あり (ただしスコープ膨張に注意、別 PR が望ましい) |
| `humanizeDetail` の UUID パターン分岐文言は API レスポンス由来の特殊ケース | 個別キー `settings.profile.user_not_found_help` で catalog 化、UUID 検出ロジックはコード側に残す |
| Datasources `new` の S3 認証情報入力 placeholder | 技術的識別子なので英語固定 (catalog 化スコープ外) でよい |
| Settings の password 強度要件メッセージ | UI 表示分のみ catalog 化、API の `password_too_short` 等は既に `api-errors` namespace でカバー済み |

---

## 8. PR-G (Phase 3g): Teams 全画面の catalog 化

**Status**: planned (PR-F merge 後に着手)。

### 8.0 対象 + 規模

| ファイル | LOC | sub-namespace |
|---|---|---|
| `app/src/app/teams/page.tsx` | 324 | `teams.list` |
| `app/src/app/teams/[id]/page.tsx` | 758 | `teams.detail` |
| **合計** | **~1082** | |

`accept-invitation` は Phase 3c (`auth.invitation`) で対応済みのため本 PR 対象外。

### 8.1 File Structure (差分)

```
app/
  src/
    i18n/config.ts                    # MOD: NAMESPACES に "teams" を追加
    locales/
      en/teams.json                   # NEW: list / detail (members / invite dialog 含む)
      ja/teams.json                   # NEW
    app/teams/{page,[id]/page}.tsx    # MOD
```

### 8.2 Task 1: namespace 追加

### 8.3 Task 2: `/teams` (list)

- ヘッダ + 新規作成ダイアログ (name / slug 入力)
- 空状態 (`team-empty-state`)
- カード形式の team 一覧 (`team-card`)

ICU 候補: `list.member_count`: `{count} 人のメンバー` 等。

### 8.4 Task 3: `/teams/[id]` (detail) 

758 LOC でこの PR の中心。内容:
- ヘッダ + 編集 / 削除アクション
- メンバー一覧 + ロール変更 / 削除
- 招待管理 (発行 / 取り消し / コピー)
- タイルセット紐付け一覧

ICU 候補:
- `detail.member_role`: `{role}` (admin/member 等の表示)
- `detail.invitation_link_template`: `{baseUrl}/accept-invitation?token={token}` (catalog 化不要、URL 構築のみ)
- `detail.remove_member_confirm`: `"{name}" をチームから削除しますか？`
- `detail.tilesets_count`: `{count} 件のタイルセット`

### 8.5 Task 4: date locale + 検証 + PR 作成

招待の有効期限表示、メンバー追加日など複数の日付がある。Phase 3d で確立した `useLocale()` ベースの `dateLocale` パターンで統一。

検証: tsc / vitest (7 namespace × en/ja keysets PASS) / build / i18n-guard / playwright list。

### 8.6 リスク

| リスク | 緩和 |
|---|---|
| team detail が複雑 (members / invitations / tilesets の 3 セクション) | sub-section ごとに `detail.members.*` `detail.invitations.*` `detail.tilesets.*` の 3 階層構造で整理 |
| 招待 link コピー時の "コピーしました" toast | 既存 `common` namespace に toast 用 key を追加するか、`teams.detail.*` に閉じ込めるか判断。後者で十分 |
| ロール / ステータス バッジ (admin / member / pending) | UI ラベルは catalog 化、内部値はそのまま |

---

PR-H (仕上げ — 詳細は最終 PR 着手時に追記):
- `api-errors.ts:JA_MESSAGES` を撤去し `useTranslations("api-errors")` 経由に切替
- `eslint-plugin-i18next` 試験導入 (ノイズが多ければ外す判断)
- 過渡期に残った hardcoded JP の最終洗い出し

---

## 9. 全体リスクと緩和

| リスク | 緩和 |
|---|---|
| Next.js 16 で `proxy.ts` の next-intl 統合が docs と乖離 | `next-intl/plugin` の最新 README 確認、build/dev で実機確認 |
| 既存日本語ユーザーが英語表示になる | proxy.ts で cookie 未設定 + Accept-Language=ja なら ja を採用、明示切替まで JA 維持 |
| catalog キーの命名 bikeshed | namespace = ファイル分割で domain 単位、キーは `<page>.<section>.<element>` の 3 階層を基本にし、深すぎる入れ子は禁止 |
| `api-errors.ts:JA_MESSAGES` の二重管理 | PR-H で catalog 一本化、過渡期は両方を維持して `translateApiError` が catalog 優先 → JA_MESSAGES fallback の順 |
| Vitest が dynamic import で全 namespace を読めない | catalog ファイル名を `i18n/config.ts:NAMESPACES` 一覧と一致させ、テスト内で `await import(...)` を index 経由で |
| 巨大 PR でレビューが困難 | 上記 8 PR 分割。PR-A は infrastructure のみで小さく |

---

## 10. Self-Review メモ

- ✓ Issue #107 のスコープ 4 セクション (基盤 / ロケール解決 / UI / API エラー連携 / CI) を PR-A〜H にカバー
- ✓ Phase 2b 統合: `api-errors.ts:JA_MESSAGES` を PR-A で catalog 化、PR-H で完全移管
- ⚠ 8 PR は多いが、各 PR が独立で merge 可能・review しやすいサイズに収めるため。慣れたら結合可
- ⚠ `proxy.ts` 統合は Next.js 16 の挙動を未確認 (docs 通りで動くか実機検証必須)
- ✓ DB migration: 本番への適用手順は `docs/POSTGRES_SETUP.md` に追記する想定 (PR-A の Step 5 で言及)
