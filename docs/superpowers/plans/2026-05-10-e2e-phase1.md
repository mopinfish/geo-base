# E2E Phase 1: Foundation + Smoke + PR CI Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Admin UI（`app/`）に Playwright ベースの E2E テスト基盤を構築し、smoke 10 件と PR トリガの CI ワークフローを稼働させる。

**Architecture:** フルローカルスタック（Postgres + Redis + FastAPI + Next.js production build + Playwright Chromium）。認証は globalSetup で API 経由ログイン → storageState 共有。DB は専用 `geo_base_e2e` を `POST /api/test/reset`（`E2E_MODE=1` ガード）でリセット。シードは API 経由のファクトリ関数。

**Tech Stack:** `@playwright/test` (Chromium), TypeScript, FastAPI test_helpers router, GitHub Actions services (postgis-3-postgresql, redis), composite action.

**Spec:** [`docs/superpowers/specs/2026-05-10-e2e-testing-design.md`](../specs/2026-05-10-e2e-testing-design.md)
**Issue:** #110
**Working branch:** `feat/issue-110-e2e-phase1`（`main` から切る）

---

## Pre-task: ブランチ準備

```bash
# main 最新を取得して新規ブランチを切る
git checkout main
git pull origin main
git checkout -b feat/issue-110-e2e-phase1
```

以降のすべてのコミットはこのブランチで行う。

---

## Task 1: `geo_base_e2e` 専用 DB を作成する

**Files:**
- Create: `docker/postgis-init/98_create_e2e_db.sh`
- Modify: `docker/docker-compose.yml`（コメント追記のみ、機能変更なし）

E2E 用に独立した DB を持つ。`99_create_test_db.sh`（pytest 用）と同じパターンで初回起動時にスキーマ複製する。

- [ ] **Step 1: 既存スクリプトを確認**

```bash
cat docker/postgis-init/99_create_test_db.sh
```

期待: `geo_base_test` を作成し `pg_dump --schema-only` でスキーマをコピーするスクリプトが表示される。

- [ ] **Step 2: 新スクリプトを作成**

```bash
cat > docker/postgis-init/98_create_e2e_db.sh <<'EOF'
#!/bin/bash
# Create geo_base_e2e database with the same schema as geo_base.
#
# Runs after all SQL init scripts (01_*..09_*) have populated the dev DB.
# Playwright E2E tests use this DB so that pytest's TRUNCATE fixtures
# (geo_base_test) and dev data (geo_base) are never touched.
#
# Numbered 98_ to run before 99_create_test_db.sh (alphabetical order).
#
# This script only runs on first container init (when the data volume is
# fresh). For existing volumes, create the DB manually:
#
#   docker compose -f docker/docker-compose.yml exec postgis \
#     psql -U postgres -c "CREATE DATABASE geo_base_e2e;"
#   docker compose -f docker/docker-compose.yml exec postgis bash -c \
#     'pg_dump -U postgres --schema-only --no-owner --no-acl geo_base \
#        | psql -U postgres -d geo_base_e2e'

set -euo pipefail

E2E_DB="${E2E_DATABASE_NAME:-geo_base_e2e}"
DEV_DB="${POSTGRES_DB:-geo_base}"

echo "[init] Creating E2E database: ${E2E_DB}"

psql -v ON_ERROR_STOP=1 -U "${POSTGRES_USER}" -d postgres <<-EOSQL
	SELECT 'CREATE DATABASE ${E2E_DB}'
	WHERE NOT EXISTS (SELECT FROM pg_database WHERE datname = '${E2E_DB}')\gexec
EOSQL

echo "[init] Cloning schema from ${DEV_DB} to ${E2E_DB}"

pg_dump -U "${POSTGRES_USER}" --schema-only --no-owner --no-acl "${DEV_DB}" \
	| psql -v ON_ERROR_STOP=1 -U "${POSTGRES_USER}" -d "${E2E_DB}"

echo "[init] E2E database ready: ${E2E_DB}"
EOF
chmod +x docker/postgis-init/98_create_e2e_db.sh
```

- [ ] **Step 3: 既存ボリュームでも実行できるよう手動コマンドを記録**

`docker/docker-compose.yml` のヘッダーコメント末尾に追記（既存 compose ボリュームを保持したまま E2E DB を追加する手順）。

`docker/docker-compose.yml` の冒頭コメントブロック（`# Note: The API server is run locally...` の直後）に以下を追加:

```yaml
#
# Adding E2E DB to an existing volume:
#   docker compose -f docker/docker-compose.yml exec postgis \
#     psql -U postgres -c "CREATE DATABASE geo_base_e2e;"
#   docker compose -f docker/docker-compose.yml exec postgis bash -c \
#     'pg_dump -U postgres --schema-only --no-owner --no-acl geo_base \
#        | psql -U postgres -d geo_base_e2e'
```

- [ ] **Step 4: 既存環境で E2E DB を作成**

ローカルの既存 volume を保持したまま手動で E2E DB を作る:

```fish
docker compose -f docker/docker-compose.yml exec postgis \
  psql -U postgres -c "CREATE DATABASE geo_base_e2e;"

docker compose -f docker/docker-compose.yml exec postgis bash -c \
  'pg_dump -U postgres --schema-only --no-owner --no-acl geo_base \
     | psql -U postgres -d geo_base_e2e'
```

期待: `[init] E2E database ready: geo_base_e2e` 相当の成功出力。テーブルが存在することを確認:

```fish
docker compose -f docker/docker-compose.yml exec postgis \
  psql -U postgres -d geo_base_e2e -c "\dt"
```

期待: `users`, `tilesets`, `features`, `datasources`, `teams`, `api_keys` 等が表示される。

- [ ] **Step 5: コミット**

```bash
git add docker/postgis-init/98_create_e2e_db.sh docker/docker-compose.yml
git commit -m "feat(docker): geo_base_e2e DB を初回起動時に自動作成

E2E テスト専用の DB を pytest 用 geo_base_test とは別に分離して持つ。
既存ボリュームでも復元できるよう手動コマンドを compose ヘッダにも記録。"
```

---

## Task 2: `cmd_create_admin` に `--password` フラグを追加する

**Files:**
- Modify: `api/lib/auth/cli.py`
- Test: `api/tests/test_auth_cli.py`（新規）

`globalSetup.ts` から非対話で admin を作るために、`getpass` の代わりにフラグから読めるようにする。**フラグなしの既存挙動は壊さない**こと。

- [ ] **Step 1: 失敗テストを書く**

`api/tests/test_auth_cli.py` を新規作成:

```python
"""Tests for `lib.auth.cli` non-interactive password handling.

Issue #110: globalSetup から `--password` で create-admin を呼べるようにする。
既存の `getpass` 経由の対話入力動作も維持されること。
"""

import asyncio
import os
from unittest.mock import AsyncMock, MagicMock, patch

import pytest


@pytest.mark.asyncio
async def test_cmd_create_admin_uses_password_flag_when_provided():
    """`--password 'foo'` が渡されたとき getpass を呼ばずにそれを使う。"""
    from lib.auth import cli

    args = MagicMock()
    args.email = "e2e-admin@test.local"
    args.password = "E2E-pass-1!"
    args.name = "E2E Admin"

    mock_provider = MagicMock()
    mock_provider.create_user = AsyncMock(return_value=MagicMock(id="user-1", email=args.email))

    with patch("lib.auth.cli.get_auth_provider", return_value=mock_provider):
        with patch("lib.auth.cli.getpass.getpass") as mock_getpass:
            with patch("builtins.input") as mock_input:
                await cli.cmd_create_admin(args)

    mock_getpass.assert_not_called()
    mock_input.assert_not_called()
    mock_provider.create_user.assert_awaited_once()
    call_kwargs = mock_provider.create_user.await_args.kwargs
    assert call_kwargs["email"] == "e2e-admin@test.local"
    assert call_kwargs["password"] == "E2E-pass-1!"
    assert call_kwargs["name"] == "E2E Admin"


@pytest.mark.asyncio
async def test_cmd_create_admin_falls_back_to_getpass_when_password_flag_absent():
    """`--password` が None のときは従来どおり getpass で対話入力する。"""
    from lib.auth import cli

    args = MagicMock()
    args.email = "interactive@test.local"
    args.password = None
    args.name = None

    mock_provider = MagicMock()
    mock_provider.create_user = AsyncMock(return_value=MagicMock(id="u-2", email=args.email))

    with patch("lib.auth.cli.get_auth_provider", return_value=mock_provider):
        with patch("lib.auth.cli.getpass.getpass", side_effect=["pw", "pw"]) as mock_getpass:
            with patch("builtins.input", return_value=""):
                await cli.cmd_create_admin(args)

    assert mock_getpass.call_count == 2
    call_kwargs = mock_provider.create_user.await_args.kwargs
    assert call_kwargs["password"] == "pw"
```

- [ ] **Step 2: テストが失敗することを確認**

```fish
cd api
set -x TEST_DATABASE_URL postgresql://postgres:postgres@localhost:5432/geo_base_test
uv run pytest tests/test_auth_cli.py -v
```

期待: FAIL — `args.password` 属性は現在 cli.py で参照されていないので、最初のケースでも `getpass` が呼ばれるはず。

- [ ] **Step 3: cli.py を修正**

`api/lib/auth/cli.py` の `cmd_create_admin` を以下のように修正（既存の `password = getpass.getpass(...)` 行のブロックを置換）:

```python
async def cmd_create_admin(args):
    from . import get_auth_provider
    provider = get_auth_provider()

    email = args.email

    # `--password` フラグが渡されたら getpass をスキップする (issue #110)。
    # globalSetup 等の非対話実行から create-admin を冪等に呼ぶための逃げ道。
    # 通常の対話実行では従来どおり getpass で 2 回入力させて取り違いを防ぐ。
    if getattr(args, "password", None):
        password = args.password
    else:
        password = getpass.getpass("Password: ")
        password_confirm = getpass.getpass("Confirm password: ")
        if password != password_confirm:
            print("Passwords do not match", file=sys.stderr)
            sys.exit(1)

    name = getattr(args, "name", None) or input("Name (optional): ").strip() or None
```

そして `main()` の `create-admin` parser 定義を以下のように修正（`--email` の直後に追加）:

```python
    p = sub.add_parser("create-admin", help="Create initial admin user")
    p.add_argument("--email", required=True)
    p.add_argument(
        "--password",
        help="非対話モード用（CI/globalSetup）。指定すると getpass のプロンプトをスキップする。",
    )
    p.add_argument(
        "--name",
        help="非対話モード用。指定するとプロンプトをスキップする。",
    )
    p.set_defaults(func=cmd_create_admin)
```

- [ ] **Step 4: テストがパスすることを確認**

```fish
cd api
uv run pytest tests/test_auth_cli.py -v
```

期待: 2 件 PASS。

- [ ] **Step 5: 既存テストを壊していないことを確認**

```fish
cd api
uv run pytest tests/ -v --no-header -q
```

期待: すべて PASS（または元から skip されているもののみ skip）。

- [ ] **Step 6: 手動で動作確認**

```fish
cd api
set -x DATABASE_URL postgresql://postgres:postgres@localhost:5432/geo_base_e2e
set -x JWT_SECRET test-secret-do-not-use-in-prod
uv run python -m lib.auth.cli create-admin \
  --email e2e-admin@test.local \
  --password 'E2E-pass-1!' \
  --name 'E2E Admin'
```

期待: `OK Admin user created: <uuid>` が出る（既に存在するなら `User with email ... already exists`）。

- [ ] **Step 7: コミット**

```bash
git add api/lib/auth/cli.py api/tests/test_auth_cli.py
git commit -m "feat(api): create-admin に --password / --name フラグを追加 (#110)

E2E globalSetup から非対話で admin を作るために、getpass を bypass する
逃げ道を追加。フラグ未指定時は従来どおり対話入力。"
```

---

## Task 3: `POST /api/test/reset` エンドポイントを追加する（E2E_MODE ガード付き）

**Files:**
- Create: `api/lib/routers/test_helpers.py`
- Modify: `api/lib/main.py`
- Test: `api/tests/test_test_helpers.py`（新規）

**重要なセキュリティ要件**: 環境変数 `E2E_MODE=1` でない限り、ルーター自体が登録されないこと。本番には絶対に存在してはならない。二重防御として、ハンドラ内でも `assert os.getenv("E2E_MODE") == "1"` を実行する。

- [ ] **Step 1: 失敗テストを書く**

`api/tests/test_test_helpers.py` を新規作成:

```python
"""Tests for /api/test/reset and the E2E_MODE gate.

Issue #110: 本番には絶対に出ない E2E 用ルーター。

二重ガードを検証:
1. E2E_MODE が unset/falsy のときは main.py がルーターを include しない。
2. E2E_MODE=1 のときだけ POST /api/test/reset が動作する。
"""

import os

import pytest
from fastapi.testclient import TestClient


def _make_app(monkeypatch, e2e_mode: str | None):
    """`E2E_MODE` を変えた状態で `lib.main` を import し直して app を返す。"""
    if e2e_mode is None:
        monkeypatch.delenv("E2E_MODE", raising=False)
    else:
        monkeypatch.setenv("E2E_MODE", e2e_mode)

    # main の import 時にルーター登録判定が走るので、毎回 reload する
    import importlib
    import lib.main as main_module

    importlib.reload(main_module)
    return main_module.app


def test_reset_endpoint_is_404_when_e2e_mode_unset(monkeypatch):
    """E2E_MODE が unset なら /api/test/reset は 404。"""
    app = _make_app(monkeypatch, None)
    client = TestClient(app)
    res = client.post("/api/test/reset")
    assert res.status_code == 404


def test_reset_endpoint_is_404_when_e2e_mode_falsy(monkeypatch):
    """E2E_MODE=0 など truthy 以外でも 404。"""
    app = _make_app(monkeypatch, "0")
    client = TestClient(app)
    res = client.post("/api/test/reset")
    assert res.status_code == 404


def test_reset_endpoint_responds_when_e2e_mode_enabled(monkeypatch):
    """E2E_MODE=1 のとき /api/test/reset は 200 を返す。"""
    app = _make_app(monkeypatch, "1")
    client = TestClient(app)
    res = client.post("/api/test/reset")
    # DB が geo_base_e2e でない場合は 400 で abort する想定（後段の test と重複しない範囲で 200 or 400 のみ許容）
    assert res.status_code in (200, 400)


def test_reset_handler_aborts_on_wrong_database(monkeypatch):
    """DATABASE_URL の DB 名が geo_base_e2e で始まらないなら 400 で abort。"""
    monkeypatch.setenv(
        "DATABASE_URL",
        "postgresql://postgres:postgres@localhost:5432/geo_base",
    )
    app = _make_app(monkeypatch, "1")
    client = TestClient(app)
    res = client.post("/api/test/reset")
    assert res.status_code == 400
    assert "geo_base_e2e" in res.json().get("detail", "").lower() or "geo_base_e2e" in str(res.json())
```

- [ ] **Step 2: テストが失敗することを確認**

```fish
cd api
set -x TEST_DATABASE_URL postgresql://postgres:postgres@localhost:5432/geo_base_test
uv run pytest tests/test_test_helpers.py -v
```

期待: 全て FAIL —ルーターも main.py の分岐もまだ存在しないため。

- [ ] **Step 3: ルーターを実装**

`api/lib/routers/test_helpers.py` を新規作成:

```python
"""E2E テスト専用ヘルパー（環境変数 E2E_MODE=1 のときだけ登録される）。

Issue #110: Playwright から DB を冪等にリセットするための逃げ道。
本番には絶対に出してはいけない。

二重ガード:
1. lib.main で `E2E_MODE` が truthy でない限り include_router しない。
2. このモジュール内のハンドラでも `assert os.getenv("E2E_MODE") == "1"` を実行する。
3. ハンドラは `DATABASE_URL` の DB 名が `geo_base_e2e` で始まらないなら 400 で abort。
"""

import os
from urllib.parse import urlparse

from fastapi import APIRouter, HTTPException

from lib.database import get_connection

router = APIRouter(prefix="/api/test", tags=["test-helpers"])


# DB 名チェックの prefix。ワーカー並列対応 (Phase 2) で `geo_base_e2e_w0` 等の
# サフィックスが付くケースを許容するため `startswith` で判定する。
_E2E_DB_PREFIX = "geo_base_e2e"


# Playwright からは下記テーブルを毎テストファイルの beforeAll で空にする想定。
# users と refresh_tokens は globalSetup で作った admin を保持するので touch しない。
_RESETTABLE_TABLES = [
    # 依存順 (foreign key を考慮): 子から親へ
    "team_invitations",
    "team_members",
    "teams",
    "api_keys",
    "features",
    "tilesets",
    "datasources",
    "password_reset_tokens",
    "login_attempts",
]


def _assert_e2e_mode_or_die() -> None:
    """ハンドラ突入時の二重防御。本番に出ても fail-closed にする。"""
    if os.getenv("E2E_MODE") != "1":
        raise HTTPException(status_code=404, detail="Not Found")


def _assert_e2e_database_or_die() -> None:
    """DATABASE_URL の DB 名が geo_base_e2e で始まらない場合は abort する。

    `geo_base` (dev) / `geo_base_test` (pytest) を誤って truncate するのを防ぐ。
    """
    db_url = os.getenv("DATABASE_URL", "")
    parsed = urlparse(db_url)
    db_name = (parsed.path or "").lstrip("/")
    if not db_name.startswith(_E2E_DB_PREFIX):
        raise HTTPException(
            status_code=400,
            detail=(
                f"Refusing to reset: DATABASE_URL must point to a "
                f"{_E2E_DB_PREFIX}* database (got '{db_name}')"
            ),
        )


@router.post("/reset")
def reset_database():
    """主要テーブルを TRUNCATE する。users / refresh_tokens は保持する。"""
    _assert_e2e_mode_or_die()
    _assert_e2e_database_or_die()

    truncated = []
    with get_connection() as conn:
        with conn.cursor() as cur:
            for table in _RESETTABLE_TABLES:
                cur.execute(
                    f"TRUNCATE TABLE {table} RESTART IDENTITY CASCADE"
                )
                truncated.append(table)
        conn.commit()
    return {"truncated": truncated}
```

- [ ] **Step 4: main.py で条件付き登録**

`api/lib/main.py` の router import 群の最後に追加:

```python
# E2E テスト用のルーター（環境変数 E2E_MODE=1 のときだけ登録される）。
# 本番には絶対に出してはいけない。詳細は lib/routers/test_helpers.py を参照。
if os.getenv("E2E_MODE") == "1":
    from lib.routers.test_helpers import router as test_helpers_router  # noqa: E402
```

そして `app.include_router(api_keys_router)` の直後に追加:

```python
if os.getenv("E2E_MODE") == "1":
    app.include_router(test_helpers_router)
```

ファイル先頭の `import` ブロックに `import os` を追加（既存なら不要）。

- [ ] **Step 5: テストがパスすることを確認**

```fish
cd api
uv run pytest tests/test_test_helpers.py -v
```

期待: 4 件 PASS。

- [ ] **Step 6: 手動で動作確認**

別ターミナルで API を `E2E_MODE=1` 付きで起動:

```fish
cd api
set -x DATABASE_URL postgresql://postgres:postgres@localhost:5432/geo_base_e2e
set -x JWT_SECRET test-secret-do-not-use-in-prod
set -x E2E_MODE 1
uv run uvicorn lib.main:app --port 8000
```

別ターミナルから:

```fish
curl -X POST http://localhost:8000/api/test/reset
```

期待: `{"truncated": ["team_invitations", ...]}`。

`E2E_MODE` を unset で再起動して同じ curl を叩く:

```fish
curl -i -X POST http://localhost:8000/api/test/reset
```

期待: HTTP 404。

- [ ] **Step 7: コミット**

```bash
git add api/lib/main.py api/lib/routers/test_helpers.py api/tests/test_test_helpers.py
git commit -m "feat(api): E2E_MODE=1 限定の POST /api/test/reset を追加 (#110)

Playwright から DB を冪等にリセットする逃げ道。三重防御:
1. main.py がルーターを include しない (E2E_MODE!=1)
2. ハンドラ内で assert E2E_MODE==1
3. DATABASE_URL の DB 名が geo_base_e2e で始まらないなら 400 で abort"
```

---

## Task 4: Playwright を `app/` に導入する

**Files:**
- Modify: `app/package.json`
- Create: `app/playwright.config.ts`
- Modify: `app/.gitignore`

- [ ] **Step 1: 依存関係を追加**

```fish
cd app
npm install -D @playwright/test
npx playwright install --with-deps chromium
```

期待: `package.json` の `devDependencies` に `@playwright/test` が増え、Chromium バイナリがダウンロードされる。

- [ ] **Step 2: `playwright.config.ts` を作成**

`app/playwright.config.ts` を新規作成:

```typescript
import { defineConfig, devices } from "@playwright/test";

/**
 * E2E test configuration (Issue #110, Phase 1).
 *
 * - 単一の baseURL に対して Playwright を実行する。
 * - 認証が必要なテストは `authenticated` project で `storageState` を使う。
 * - 認証フロー自体のテストは `unauthenticated` project で storageState なし。
 *
 * 各種パスは `tests/e2e/` 配下に集約する。
 * Phase 2 でワーカー並列化 (workers: 4 + ワーカー別 DB) に拡張する。
 */
export default defineConfig({
  testDir: "./tests/e2e",
  fullyParallel: false, // Phase 1 は単一 DB のため並列化しない
  workers: 1,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  reporter: process.env.CI ? [["github"], ["html", { open: "never" }]] : "list",

  use: {
    baseURL: process.env.PLAYWRIGHT_BASE_URL || "http://localhost:3000",
    trace: "on-first-retry",
    video: "retain-on-failure",
  },

  globalSetup: require.resolve("./tests/e2e/globalSetup.ts"),

  projects: [
    {
      name: "unauthenticated",
      testMatch: /tests\/e2e\/auth\/.*\.spec\.ts/,
      use: { ...devices["Desktop Chrome"] },
    },
    {
      name: "authenticated",
      testIgnore: /tests\/e2e\/auth\/.*\.spec\.ts/,
      use: {
        ...devices["Desktop Chrome"],
        storageState: "playwright/.auth/admin.json",
      },
    },
  ],

  // ローカル/CI ともに Next.js を `next start` で起動する。
  // CI では composite action で起動済みなので reuseExistingServer: true。
  webServer: process.env.CI
    ? undefined
    : {
        command: "npm run start",
        url: "http://localhost:3000",
        reuseExistingServer: true,
        timeout: 120_000,
      },
});
```

- [ ] **Step 3: `package.json` の scripts を更新**

`app/package.json` の `scripts` セクションを置換:

```json
"scripts": {
  "dev": "next dev",
  "build": "next build",
  "start": "next start",
  "lint": "eslint",
  "test": "vitest run",
  "test:watch": "vitest",
  "test:e2e": "playwright test",
  "test:e2e:smoke": "playwright test --grep @smoke",
  "test:e2e:ui": "playwright test --ui"
}
```

- [ ] **Step 4: `.gitignore` を更新**

`app/.gitignore` に以下を追加:

```
# Playwright
/playwright/.auth/
/test-results/
/playwright-report/
```

- [ ] **Step 5: 動作確認**

`tests/e2e/globalSetup.ts` がまだ無いので config だけは parse できることを確認:

```fish
cd app
npx playwright test --list 2>&1 | head -20
```

期待: globalSetup の解決エラーが出る（次の Task で実装するので想定どおり）。

- [ ] **Step 6: コミット**

```bash
git add app/package.json app/package-lock.json app/playwright.config.ts app/.gitignore
git commit -m "feat(app): Playwright を導入し playwright.config.ts を追加 (#110)

unauthenticated / authenticated の 2 project 構成。
Phase 1 は workers: 1、Phase 2 でワーカー別 DB に拡張予定。"
```

---

## Task 5: `globalSetup.ts` で admin ユーザーを作成・ログインして storageState を保存する

**Files:**
- Create: `app/tests/e2e/globalSetup.ts`
- Create: `app/tests/e2e/utils/wait-for-server.ts`

- [ ] **Step 1: `wait-for-server.ts` を作成**

`app/tests/e2e/utils/wait-for-server.ts` を新規作成:

```typescript
/**
 * Polling helper to wait until a URL responds with 2xx.
 *
 * `request` instead of `node:fetch` so it works under Playwright's process
 * without polyfill considerations.
 */
import { request as createRequest } from "@playwright/test";

export async function waitForServer(url: string, timeoutMs = 60_000): Promise<void> {
  const start = Date.now();
  const ctx = await createRequest.newContext();
  try {
    while (Date.now() - start < timeoutMs) {
      try {
        const res = await ctx.get(url);
        if (res.ok()) return;
      } catch {
        // ignore connection refused / etc.
      }
      await new Promise((r) => setTimeout(r, 500));
    }
    throw new Error(`Timeout waiting for ${url} to be ready (${timeoutMs}ms)`);
  } finally {
    await ctx.dispose();
  }
}
```

- [ ] **Step 2: `globalSetup.ts` を作成**

`app/tests/e2e/globalSetup.ts` を新規作成:

```typescript
/**
 * Playwright globalSetup (Issue #110, Phase 1).
 *
 * 1. API と Next.js の起動を待つ
 * 2. `python -m lib.auth.cli create-admin --password ...` で admin を冪等作成
 * 3. POST /api/auth/login でトークンを取得
 * 4. storageState を `playwright/.auth/admin.json` に保存
 */
import { execSync } from "node:child_process";
import { mkdirSync } from "node:fs";
import path from "node:path";
import { request, type FullConfig } from "@playwright/test";

import { waitForServer } from "./utils/wait-for-server";

export const E2E_ADMIN_EMAIL = "e2e-admin@test.local";
export const E2E_ADMIN_PASSWORD = "E2E-pass-1!";
export const E2E_ADMIN_NAME = "E2E Admin";

const API_BASE = process.env.PLAYWRIGHT_API_BASE_URL || "http://localhost:8000";
const APP_BASE = process.env.PLAYWRIGHT_BASE_URL || "http://localhost:3000";
const AUTH_DIR = path.resolve(__dirname, "../../playwright/.auth");

async function ensureAdminUser(): Promise<void> {
  // CLI 経由で admin を冪等に作成する。既に存在するなら "already exists" 出力で
  // 終了コード 1 になるため、それは握りつぶす。失敗の本当のシグナルは
  // 直後のログイン試行で検出する。
  const cmd = [
    "uv run python -m lib.auth.cli create-admin",
    `--email ${E2E_ADMIN_EMAIL}`,
    `--password '${E2E_ADMIN_PASSWORD}'`,
    `--name '${E2E_ADMIN_NAME}'`,
  ].join(" ");

  try {
    execSync(cmd, {
      cwd: path.resolve(__dirname, "../../../api"),
      stdio: "inherit",
      env: { ...process.env, PYTHONUNBUFFERED: "1" },
    });
  } catch {
    // 既存ユーザー時は exit 1 になる。ログインで本当に動くかを確認するのでここでは無視。
  }
}

async function loginAndSaveState(): Promise<void> {
  mkdirSync(AUTH_DIR, { recursive: true });

  const ctx = await request.newContext({ baseURL: APP_BASE });
  // Cookie を保持して /api/auth/login 経由でセッションを成立させる。
  // Next.js の rewrites を通して /api/auth/login が API に proxy される構成に依存。
  // ローカルの Next.js dev では `next.config.ts` で /api 配下を API_URL に rewrite している。
  const res = await ctx.post(`${API_BASE}/api/auth/login`, {
    data: { email: E2E_ADMIN_EMAIL, password: E2E_ADMIN_PASSWORD },
  });
  if (!res.ok()) {
    throw new Error(`Failed to login as ${E2E_ADMIN_EMAIL}: ${res.status()} ${await res.text()}`);
  }
  // refresh cookie がレスポンスに乗る。それを保存。
  await ctx.storageState({ path: path.join(AUTH_DIR, "admin.json") });
  await ctx.dispose();
}

export default async function globalSetup(_config: FullConfig): Promise<void> {
  await waitForServer(`${API_BASE}/api/health`);
  await waitForServer(APP_BASE);
  await ensureAdminUser();
  await loginAndSaveState();
}
```

- [ ] **Step 3: 動作確認**

API（`E2E_MODE=1` + `geo_base_e2e`）と Next.js を起動した状態で:

```fish
cd app
npx playwright test --list
```

期待: ファイルレベルのテスト一覧が表示される（まだテストファイルが無いので "Found 0 tests" でも OK）。globalSetup 自体がエラーなく解決されること。

- [ ] **Step 4: コミット**

```bash
git add app/tests/e2e/globalSetup.ts app/tests/e2e/utils/wait-for-server.ts
git commit -m "feat(app): Playwright globalSetup で admin 自動作成 + storageState 保存 (#110)

CLI で admin を冪等作成し、API 直接 POST でトークン取得 →
playwright/.auth/admin.json に保存。authenticated project が再利用する。"
```

---

## Task 6: API クライアント・ファクトリ・DB リセットヘルパーを作る

**Files:**
- Create: `app/tests/e2e/fixtures/api-client.ts`
- Create: `app/tests/e2e/fixtures/factories.ts`
- Create: `app/tests/e2e/utils/reset-db.ts`

- [ ] **Step 1: `api-client.ts` を作成**

`app/tests/e2e/fixtures/api-client.ts` を新規作成:

```typescript
/**
 * 認証付き API クライアント。
 *
 * globalSetup で保存した storageState の cookie をそのまま `request` context に
 * 流し込んで使う。各テストファイルから:
 *
 *   const api = await createApiClient();
 *   const tileset = await api.createTileset({ name: "..." });
 */
import path from "node:path";
import { request, type APIRequestContext } from "@playwright/test";

const API_BASE = process.env.PLAYWRIGHT_API_BASE_URL || "http://localhost:8000";
const STORAGE_STATE = path.resolve(
  __dirname,
  "../../../playwright/.auth/admin.json",
);

export async function createApiClient(): Promise<APIRequestContext> {
  return request.newContext({
    baseURL: API_BASE,
    storageState: STORAGE_STATE,
    extraHTTPHeaders: { "Content-Type": "application/json" },
  });
}
```

- [ ] **Step 2: `reset-db.ts` を作成**

`app/tests/e2e/utils/reset-db.ts` を新規作成:

```typescript
/**
 * DB リセットヘルパー: POST /api/test/reset を呼ぶ。
 *
 * 安全装置: API 側でも DATABASE_URL の DB 名チェックを行うが、念のため
 * クライアント側でも `PLAYWRIGHT_API_BASE_URL` のホストが localhost / 127.0.0.1
 * 以外なら refuse する。production を誤爆しないため。
 */
import { createApiClient } from "../fixtures/api-client";

export async function resetDatabase(): Promise<void> {
  const apiBase = process.env.PLAYWRIGHT_API_BASE_URL || "http://localhost:8000";
  const url = new URL(apiBase);
  if (!["localhost", "127.0.0.1"].includes(url.hostname)) {
    throw new Error(
      `Refusing to call /api/test/reset against non-local host: ${url.hostname}`,
    );
  }

  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/test/reset");
    if (!res.ok()) {
      throw new Error(`Failed to reset DB: ${res.status()} ${await res.text()}`);
    }
  } finally {
    await ctx.dispose();
  }
}
```

- [ ] **Step 3: `factories.ts` を作成**

`app/tests/e2e/fixtures/factories.ts` を新規作成:

```typescript
/**
 * E2E テスト用シードファクトリ。
 *
 * 各関数は POST <endpoint> を叩いてサーバから ID 付きエンティティを返す。
 * Playwright 側に SQL を散らさず、API レイヤーで境界を持たせる方針。
 */
import { createApiClient } from "./api-client";

export interface CreatedTileset {
  id: string;
  name: string;
  type: string;
  is_public: boolean;
}

export interface CreatedDatasource {
  id: string;
  name: string;
  url: string;
  type: string;
  is_public: boolean;
}

export interface CreatedTeam {
  id: string;
  name: string;
  slug: string;
}

export async function createTileset(input: {
  name: string;
  type?: "vector" | "raster" | "pmtiles";
  isPublic?: boolean;
}): Promise<CreatedTileset> {
  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/tilesets/", {
      data: {
        name: input.name,
        type: input.type ?? "vector",
        is_public: input.isPublic ?? false,
      },
    });
    if (!res.ok()) {
      throw new Error(`createTileset failed: ${res.status()} ${await res.text()}`);
    }
    return (await res.json()) as CreatedTileset;
  } finally {
    await ctx.dispose();
  }
}

export async function createDatasource(input: {
  name: string;
  url: string;
  type?: "pmtiles" | "cog";
  isPublic?: boolean;
}): Promise<CreatedDatasource> {
  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/datasources/", {
      data: {
        name: input.name,
        url: input.url,
        type: input.type ?? "pmtiles",
        is_public: input.isPublic ?? true,
      },
    });
    if (!res.ok()) {
      throw new Error(`createDatasource failed: ${res.status()} ${await res.text()}`);
    }
    return (await res.json()) as CreatedDatasource;
  } finally {
    await ctx.dispose();
  }
}

export async function createTeam(input: {
  name: string;
  slug?: string;
}): Promise<CreatedTeam> {
  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/teams/", {
      data: { name: input.name, slug: input.slug },
    });
    if (!res.ok()) {
      throw new Error(`createTeam failed: ${res.status()} ${await res.text()}`);
    }
    return (await res.json()) as CreatedTeam;
  } finally {
    await ctx.dispose();
  }
}
```

- [ ] **Step 4: コミット**

```bash
git add app/tests/e2e/fixtures app/tests/e2e/utils/reset-db.ts
git commit -m "feat(app): E2E 用 API クライアント / ファクトリ / DB リセットを追加 (#110)

storageState 経由の認証付き request context、tilesets/datasources/teams の
ファクトリ関数、POST /api/test/reset のラッパー。"
```

---

## Task 7: AUTH-01 / AUTH-04 — ログイン・ログアウト smoke

**Files:**
- Create: `app/tests/e2e/auth/login.spec.ts`
- Modify: `app/src/app/login/page.tsx`（`data-testid` 追加）
- Modify: `app/src/components/layout/sidebar.tsx`（ログアウトボタンに `data-testid` 追加）

- [ ] **Step 1: ログインページに `data-testid` を追加**

`app/src/app/login/page.tsx` の `<form>` 内 input/button を以下のように修正（既存属性は保持して `data-testid` のみ追加）:

```tsx
<input
  type="email" required value={email} onChange={(e) => setEmail(e.target.value)}
  placeholder="メールアドレス" className="w-full p-2 border rounded"
  data-testid="login-email"
/>
<input
  type="password" required value={password} onChange={(e) => setPassword(e.target.value)}
  placeholder="パスワード" className="w-full p-2 border rounded"
  data-testid="login-password"
/>
{error && <p className="text-red-600" data-testid="login-error">{error}</p>}
<button type="submit" disabled={loading} className="w-full p-2 bg-blue-600 text-white rounded"
  data-testid="login-submit">
  {loading ? "..." : "ログイン"}
</button>
```

- [ ] **Step 2: サイドバーのログアウトボタンに `data-testid` を追加**

`app/src/components/layout/sidebar.tsx` の `LogOut` ボタン（`onClick` で `authClient.logout()` を呼ぶ箇所）に `data-testid="sidebar-logout"` を追加する。

具体的な行は `grep -n "LogOut\|logout" app/src/components/layout/sidebar.tsx` で特定。
当該の `<button>` または `<Button>` JSX に以下を追加:

```tsx
data-testid="sidebar-logout"
```

- [ ] **Step 3: 失敗テストを書く**

`app/tests/e2e/auth/login.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import {
  E2E_ADMIN_EMAIL,
  E2E_ADMIN_PASSWORD,
} from "../globalSetup";

test.beforeAll(async () => {
  await resetDatabase();
});

test("AUTH-01 @smoke ログイン成功で / にリダイレクトされる", async ({ page }) => {
  await page.goto("/login");
  await page.getByTestId("login-email").fill(E2E_ADMIN_EMAIL);
  await page.getByTestId("login-password").fill(E2E_ADMIN_PASSWORD);
  await page.getByTestId("login-submit").click();

  await page.waitForURL("/");
  await expect(page).toHaveURL("/");
});

test("AUTH-04 @smoke ログアウトで /login に遷移しセッションがクリアされる", async ({
  page,
}) => {
  // 先にログインしておく
  await page.goto("/login");
  await page.getByTestId("login-email").fill(E2E_ADMIN_EMAIL);
  await page.getByTestId("login-password").fill(E2E_ADMIN_PASSWORD);
  await page.getByTestId("login-submit").click();
  await page.waitForURL("/");

  // ログアウト
  await page.getByTestId("sidebar-logout").click();
  await page.waitForURL(/\/login(\?|$)/);

  // 保護ルートに直接アクセスして、未認証扱いで /login に戻されることを確認
  await page.goto("/tilesets");
  await page.waitForURL(/\/login(\?|$)/);
  await expect(page).toHaveURL(/\/login(\?|$)/);
});
```

注意: AUTH-01/AUTH-04 は **`unauthenticated` project** で動かしたい。Playwright のデフォルトでは `tests/e2e/auth/**` は `unauthenticated` project にマッチする（`playwright.config.ts` の `testMatch` に従う）ため、storageState は読み込まれない。

- [ ] **Step 4: テストを実行**

API (`E2E_MODE=1` + `geo_base_e2e`) と Next.js (production build) を起動済みの前提で:

```fish
cd app
npm run build
# 別ターミナルで `npm run start` （`reuseExistingServer: true` で再利用）
# まだ未起動なら `npm run start` を別ターミナルで実行
npx playwright test tests/e2e/auth/login.spec.ts --project=unauthenticated
```

期待: AUTH-01 と AUTH-04 が PASS。失敗する場合は `--reporter=list` でログを確認。

- [ ] **Step 5: コミット**

```bash
git add app/src/app/login/page.tsx app/src/components/layout/sidebar.tsx app/tests/e2e/auth/login.spec.ts
git commit -m "test(app): AUTH-01 / AUTH-04 smoke E2E を追加 (#110)

ログイン成功とログアウト + 未認証時のリダイレクトを検証。
セレクタ用に login ページとサイドバーに data-testid を追加。"
```

---

## Task 8: DASH-01 — ダッシュボード件数表示 smoke

**Files:**
- Create: `app/tests/e2e/dashboard/dashboard.spec.ts`
- Modify: `app/src/app/page.tsx`（カウント表示要素に `data-testid` 追加）

- [ ] **Step 1: ダッシュボードに `data-testid` を追加**

`app/src/app/page.tsx` の各統計カードに `data-testid` を付与する。`stats` を表示している `<Card>` を 3 つ（tilesets / features / datasources）特定し、それぞれ:

- tilesets カウント表示要素: `data-testid="dashboard-tilesets-count"`
- features カウント表示要素: `data-testid="dashboard-features-count"`
- datasources カウント表示要素: `data-testid="dashboard-datasources-count"`

`grep -n "stats\|count\|Layers\|Map\|Database" app/src/app/page.tsx` で実装場所を特定し、表示数値を含む要素に `data-testid` を追加。

- [ ] **Step 2: 失敗テストを書く**

`app/tests/e2e/dashboard/dashboard.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createTileset, createDatasource } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
  await createTileset({ name: "smoke-tileset-1", type: "vector" });
  await createTileset({ name: "smoke-tileset-2", type: "raster" });
  await createDatasource({
    name: "smoke-ds-1",
    url: "https://example.com/sample.pmtiles",
    type: "pmtiles",
  });
});

test("DASH-01 @smoke ダッシュボードに各カウントが表示される", async ({ page }) => {
  await page.goto("/");

  // 件数要素は最終的に数字を含むので、.toBeVisible() + 数字検証
  const tilesetsCount = page.getByTestId("dashboard-tilesets-count");
  const featuresCount = page.getByTestId("dashboard-features-count");
  const datasourcesCount = page.getByTestId("dashboard-datasources-count");

  await expect(tilesetsCount).toBeVisible();
  await expect(featuresCount).toBeVisible();
  await expect(datasourcesCount).toBeVisible();

  // tilesets は 2 件作成したので、テキストに 2 以上を含む
  await expect(tilesetsCount).toHaveText(/[2-9]|\d{2,}/);
});
```

- [ ] **Step 3: テストを実行**

```fish
cd app
npx playwright test tests/e2e/dashboard/dashboard.spec.ts --project=authenticated
```

期待: DASH-01 が PASS。

- [ ] **Step 4: コミット**

```bash
git add app/src/app/page.tsx app/tests/e2e/dashboard/dashboard.spec.ts
git commit -m "test(app): DASH-01 smoke E2E を追加 (#110)

ダッシュボードの tilesets/features/datasources カウント表示を検証。"
```

---

## Task 9: TS-01 / TS-07 — タイルセット一覧と作成 smoke

**Files:**
- Create: `app/tests/e2e/tilesets/list-create.spec.ts`
- Modify: `app/src/app/tilesets/page.tsx`（要素に `data-testid` 追加）
- Modify: `app/src/app/tilesets/new/page.tsx`（要素に `data-testid` 追加）

- [ ] **Step 1: タイルセット関連ページに `data-testid` を追加**

以下のセレクタを各ページに付与:

`app/src/app/tilesets/page.tsx`:
- リストの 1 行 (table row): `data-testid="tileset-list-row"`
- 「新規作成」ボタン: `data-testid="tileset-create-link"`

`app/src/app/tilesets/new/page.tsx`:
- name 入力: `data-testid="tileset-form-name"`
- type 選択: `data-testid="tileset-form-type"`
- description 入力（あれば）: `data-testid="tileset-form-description"`
- submit ボタン: `data-testid="tileset-form-submit"`

実際のフィールド名は `app/src/components/tilesets/` の form コンポーネントを参照。

- [ ] **Step 2: 失敗テストを書く**

`app/tests/e2e/tilesets/list-create.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createTileset } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
});

test("TS-01 @smoke タイルセット一覧が表示される", async ({ page }) => {
  await createTileset({ name: "ts-list-smoke-A", type: "vector" });
  await createTileset({ name: "ts-list-smoke-B", type: "raster" });

  await page.goto("/tilesets");

  const rows = page.getByTestId("tileset-list-row");
  await expect(rows).toHaveCount(2, { timeout: 10_000 });
  await expect(page.getByText("ts-list-smoke-A")).toBeVisible();
  await expect(page.getByText("ts-list-smoke-B")).toBeVisible();
});

test("TS-07 @smoke タイルセット新規作成 → 詳細ページへ遷移", async ({ page }) => {
  await page.goto("/tilesets");
  await page.getByTestId("tileset-create-link").click();
  await page.waitForURL("/tilesets/new");

  await page.getByTestId("tileset-form-name").fill("ts-create-smoke");
  await page.getByTestId("tileset-form-submit").click();

  // 詳細ページに遷移する想定。URL パターンを 1 件マッチで確認。
  await page.waitForURL(/\/tilesets\/[^/]+(\?|$)/, { timeout: 15_000 });
  await expect(page.getByText("ts-create-smoke")).toBeVisible();
});
```

- [ ] **Step 3: テストを実行**

```fish
cd app
npx playwright test tests/e2e/tilesets/list-create.spec.ts --project=authenticated
```

期待: TS-01, TS-07 PASS。失敗する場合は対象 page の実装を再確認し、`getByTestId` のセレクタが要素にマッチしているかを `--debug` で検証。

- [ ] **Step 4: コミット**

```bash
git add app/src/app/tilesets app/tests/e2e/tilesets/list-create.spec.ts
git commit -m "test(app): TS-01 / TS-07 smoke E2E を追加 (#110)

タイルセット一覧表示と新規作成フローを検証。"
```

---

## Task 10: FT-01 — フィーチャー一覧 smoke

**Files:**
- Create: `app/tests/e2e/features/list.spec.ts`
- Modify: `app/src/app/features/page.tsx`（要素に `data-testid` 追加）
- Modify: `app/tests/e2e/fixtures/factories.ts`（`createFeature` を追加）

- [ ] **Step 1: features ページに `data-testid` を追加**

`app/src/app/features/page.tsx` のリスト 1 行に `data-testid="feature-list-row"` を付与。

- [ ] **Step 2: `factories.ts` に `createFeature` を追加**

`app/tests/e2e/fixtures/factories.ts` の末尾に追加:

```typescript
export interface CreatedFeature {
  id: string;
  tileset_id: string;
  layer: string;
  geometry: unknown;
  properties: Record<string, unknown>;
}

export async function createFeature(input: {
  tilesetId: string;
  layer: string;
  geometry: unknown;
  properties?: Record<string, unknown>;
}): Promise<CreatedFeature> {
  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/features/", {
      data: {
        tileset_id: input.tilesetId,
        layer: input.layer,
        geometry: input.geometry,
        properties: input.properties ?? {},
      },
    });
    if (!res.ok()) {
      throw new Error(`createFeature failed: ${res.status()} ${await res.text()}`);
    }
    return (await res.json()) as CreatedFeature;
  } finally {
    await ctx.dispose();
  }
}
```

API のフィーチャー作成エンドポイントが `POST /api/features/` でない場合は、`api/lib/routers/features.py` を確認して endpoint を合わせる（`grep -n "@router.post" api/lib/routers/features.py`）。

- [ ] **Step 3: 失敗テストを書く**

`app/tests/e2e/features/list.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createTileset, createFeature } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
  const tileset = await createTileset({ name: "ft-list-smoke", type: "vector" });
  await createFeature({
    tilesetId: tileset.id,
    layer: "points",
    geometry: { type: "Point", coordinates: [139.767, 35.681] },
    properties: { name: "Tokyo" },
  });
  await createFeature({
    tilesetId: tileset.id,
    layer: "points",
    geometry: { type: "Point", coordinates: [135.502, 34.693] },
    properties: { name: "Osaka" },
  });
});

test("FT-01 @smoke フィーチャー一覧が表示される", async ({ page }) => {
  await page.goto("/features");

  const rows = page.getByTestId("feature-list-row");
  await expect(rows).toHaveCount(2, { timeout: 10_000 });
});
```

- [ ] **Step 4: テストを実行**

```fish
cd app
npx playwright test tests/e2e/features/list.spec.ts --project=authenticated
```

期待: FT-01 PASS。

- [ ] **Step 5: コミット**

```bash
git add app/src/app/features/page.tsx app/tests/e2e/features app/tests/e2e/fixtures/factories.ts
git commit -m "test(app): FT-01 smoke E2E を追加 (#110)

フィーチャー一覧表示を検証、createFeature ファクトリも追加。"
```

---

## Task 11: FT-13 — GeoJSON drag-drop import smoke

**Files:**
- Create: `app/tests/e2e/features/import.spec.ts`
- Create: `app/tests/e2e/fixtures/sample.geojson`
- Modify: `app/src/app/features/import/page.tsx`（要素に `data-testid` 追加）

- [ ] **Step 1: サンプル GeoJSON ファイルを用意**

`app/tests/e2e/fixtures/sample.geojson` を新規作成:

```json
{
  "type": "FeatureCollection",
  "features": [
    {
      "type": "Feature",
      "geometry": { "type": "Point", "coordinates": [139.767, 35.681] },
      "properties": { "name": "Tokyo Station" }
    },
    {
      "type": "Feature",
      "geometry": { "type": "Point", "coordinates": [135.502, 34.693] },
      "properties": { "name": "Osaka Station" }
    }
  ]
}
```

- [ ] **Step 2: import ページに `data-testid` を追加**

`app/src/app/features/import/page.tsx` および関連コンポーネント（`GeoJSONDropzone` 等）に以下を追加:

- ファイル input（ドロップゾーン背後の hidden input）: `data-testid="import-file-input"`
- tileset 選択 select: `data-testid="import-tileset-select"`
- import 実行ボタン: `data-testid="import-submit"`
- 成功 toast / メッセージ: `data-testid="import-success-message"`

ドロップゾーンが直接 file をハンドリングする場合、Playwright の `setInputFiles` で動かせるよう **`<input type="file">` を必ず DOM に存在させる**こと（`hidden` 属性は OK）。

- [ ] **Step 3: 失敗テストを書く**

`app/tests/e2e/features/import.spec.ts` を新規作成:

```typescript
import path from "node:path";
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createTileset } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
});

test("FT-13 @smoke GeoJSON ファイルを drag-drop で import できる", async ({ page }) => {
  const tileset = await createTileset({ name: "ft-import-smoke", type: "vector" });

  await page.goto("/features/import");
  await page.getByTestId("import-tileset-select").selectOption(tileset.id);
  await page
    .getByTestId("import-file-input")
    .setInputFiles(path.resolve(__dirname, "../fixtures/sample.geojson"));
  await page.getByTestId("import-submit").click();

  await expect(page.getByTestId("import-success-message")).toBeVisible({
    timeout: 15_000,
  });
});
```

- [ ] **Step 4: テストを実行**

```fish
cd app
npx playwright test tests/e2e/features/import.spec.ts --project=authenticated
```

期待: FT-13 PASS。

- [ ] **Step 5: コミット**

```bash
git add app/src/app/features/import app/tests/e2e/features/import.spec.ts app/tests/e2e/fixtures/sample.geojson
git commit -m "test(app): FT-13 smoke E2E を追加 (#110)

GeoJSON ファイル import の happy path を検証。"
```

---

## Task 12: DS-01 — データソース一覧 + type フィルタ smoke

**Files:**
- Create: `app/tests/e2e/datasources/list.spec.ts`
- Modify: `app/src/app/datasources/page.tsx`（要素に `data-testid` 追加）

- [ ] **Step 1: datasources ページに `data-testid` を追加**

`app/src/app/datasources/page.tsx`:
- リスト 1 行: `data-testid="datasource-list-row"`
- type フィルタ select: `data-testid="datasource-filter-type"`

- [ ] **Step 2: 失敗テストを書く**

`app/tests/e2e/datasources/list.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createDatasource } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
  await createDatasource({
    name: "ds-pmtiles-smoke",
    url: "https://example.com/sample.pmtiles",
    type: "pmtiles",
  });
  await createDatasource({
    name: "ds-cog-smoke",
    url: "https://example.com/sample.tif",
    type: "cog",
  });
});

test("DS-01 @smoke データソース一覧と type フィルタが動く", async ({ page }) => {
  await page.goto("/datasources");

  const rows = page.getByTestId("datasource-list-row");
  await expect(rows).toHaveCount(2);

  // type=pmtiles でフィルタ
  await page.getByTestId("datasource-filter-type").selectOption("pmtiles");
  await expect(rows).toHaveCount(1);
  await expect(page.getByText("ds-pmtiles-smoke")).toBeVisible();
});
```

- [ ] **Step 3: テストを実行 + コミット**

```fish
cd app
npx playwright test tests/e2e/datasources/list.spec.ts --project=authenticated
```

期待: DS-01 PASS。

```bash
git add app/src/app/datasources/page.tsx app/tests/e2e/datasources
git commit -m "test(app): DS-01 smoke E2E を追加 (#110)

データソース一覧表示と type フィルタを検証。"
```

---

## Task 13: TM-01 — チーム一覧 smoke

**Files:**
- Create: `app/tests/e2e/teams/list.spec.ts`
- Modify: `app/src/app/teams/page.tsx`（要素に `data-testid` 追加）

- [ ] **Step 1: teams ページに `data-testid` を追加**

`app/src/app/teams/page.tsx`:
- チームカード 1 つ: `data-testid="team-card"`
- 空状態の表示要素: `data-testid="team-empty-state"`（あれば）

- [ ] **Step 2: 失敗テストを書く**

`app/tests/e2e/teams/list.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createTeam } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
  await createTeam({ name: "Team Alpha" });
  await createTeam({ name: "Team Beta" });
});

test("TM-01 @smoke チーム一覧に作成済みのチームが表示される", async ({ page }) => {
  await page.goto("/teams");

  const cards = page.getByTestId("team-card");
  await expect(cards).toHaveCount(2);
  await expect(page.getByText("Team Alpha")).toBeVisible();
  await expect(page.getByText("Team Beta")).toBeVisible();
});
```

- [ ] **Step 3: テストを実行 + コミット**

```fish
cd app
npx playwright test tests/e2e/teams/list.spec.ts --project=authenticated
```

期待: TM-01 PASS。

```bash
git add app/src/app/teams/page.tsx app/tests/e2e/teams
git commit -m "test(app): TM-01 smoke E2E を追加 (#110)

チーム一覧表示を検証。"
```

---

## Task 14: AK-01 — API キー一覧 smoke

**Files:**
- Create: `app/tests/e2e/api-keys/list.spec.ts`
- Modify: `app/src/app/api-keys/page.tsx`（要素に `data-testid` 追加）
- Modify: `app/tests/e2e/fixtures/factories.ts`（`createApiKey` を追加）

- [ ] **Step 1: api-keys ページに `data-testid` を追加**

`app/src/app/api-keys/page.tsx`:
- リスト 1 行: `data-testid="api-key-row"`
- マスク済み key 表示要素: `data-testid="api-key-masked"`

- [ ] **Step 2: `factories.ts` に `createApiKey` を追加**

`app/tests/e2e/fixtures/factories.ts` の末尾に追加:

```typescript
export interface CreatedApiKey {
  id: string;
  name: string;
  prefix: string;
  // 作成直後だけ返ってくる平文 key
  key?: string;
}

export async function createApiKey(input: {
  name: string;
  scopes?: string[];
}): Promise<CreatedApiKey> {
  const ctx = await createApiClient();
  try {
    const res = await ctx.post("/api/api-keys/", {
      data: {
        name: input.name,
        scopes: input.scopes ?? ["read"],
      },
    });
    if (!res.ok()) {
      throw new Error(`createApiKey failed: ${res.status()} ${await res.text()}`);
    }
    return (await res.json()) as CreatedApiKey;
  } finally {
    await ctx.dispose();
  }
}
```

実際のリクエスト形式は `api/lib/routers/api_keys.py` を参照。違うフィールド名なら合わせる。

- [ ] **Step 3: 失敗テストを書く**

`app/tests/e2e/api-keys/list.spec.ts` を新規作成:

```typescript
import { test, expect } from "@playwright/test";

import { resetDatabase } from "../utils/reset-db";
import { createApiKey } from "../fixtures/factories";

test.beforeAll(async () => {
  await resetDatabase();
  await createApiKey({ name: "smoke-key-1" });
  await createApiKey({ name: "smoke-key-2" });
});

test("AK-01 @smoke API キー一覧が表示される（key 値はマスクされている）", async ({
  page,
}) => {
  await page.goto("/api-keys");

  const rows = page.getByTestId("api-key-row");
  await expect(rows).toHaveCount(2);

  // マスク済み key には少なくとも `gb_live_` 等のプレフィクスや `*` が含まれる想定。
  // 完全な平文 key（`gb_live_<32 桁>` の長さ）はマスク後の表示には現れない。
  const masks = page.getByTestId("api-key-masked");
  await expect(masks.first()).toBeVisible();
  await expect(masks.first()).toContainText(/[*•]/);
});
```

- [ ] **Step 4: テストを実行 + コミット**

```fish
cd app
npx playwright test tests/e2e/api-keys/list.spec.ts --project=authenticated
```

期待: AK-01 PASS。

```bash
git add app/src/app/api-keys/page.tsx app/tests/e2e/api-keys app/tests/e2e/fixtures/factories.ts
git commit -m "test(app): AK-01 smoke E2E を追加 (#110)

API キー一覧表示とマスキングを検証、createApiKey ファクトリも追加。"
```

---

## Task 15: smoke 全 10 件をまとめて実行する

**Files:** 変更なし

すべての smoke spec を `@smoke` タグでマークしているので、`test:e2e:smoke` script で 10 件まとめて流せる。Phase 1 完了の動作確認。

- [ ] **Step 1: `test:e2e:smoke` 実行**

API（`E2E_MODE=1` + `geo_base_e2e`）と Next.js 起動済みで:

```fish
cd app
npm run test:e2e:smoke
```

期待: 全 10 件 PASS。失敗があれば原因を特定して該当 Task に戻って修正。

- [ ] **Step 2: HTML レポート確認**

```fish
npx playwright show-report
```

期待: 10 件すべて緑色で表示される。

- [ ] **Step 3: 全件総実行時間を確認**

期待: ローカルで 90 秒以内。超える場合は `playwright.config.ts` の `globalSetup` 内のサーバ待ちが長過ぎないか確認。

---

## Task 16: composite action `e2e-setup` を作る

**Files:**
- Create: `.github/actions/e2e-setup/action.yml`

CI 3 ワークフロー（Phase 1 では smoke のみ、Phase 2 で full / nightly）の重複を抑える。

- [ ] **Step 1: composite action を作成**

`.github/actions/e2e-setup/action.yml` を新規作成:

```yaml
name: "E2E setup"
description: |
  geo-base の E2E テスト用環境セットアップ。
  Postgres / Redis services はワークフロー側で `services:` ブロックで指定する前提で、
  本 action は uv / Node / Playwright のキャッシュ復元と依存解決、
  geo_base_e2e DB スキーマの初期化、admin の create を担当する。

inputs:
  python-version:
    description: "Python version"
    required: false
    default: "3.11"
  node-version:
    description: "Node version"
    required: false
    default: "20"
  postgres-host:
    description: "Postgres host (services 経由なら 'localhost' でよい)"
    required: false
    default: "localhost"
  postgres-port:
    description: "Postgres port"
    required: false
    default: "5432"
  e2e-database-url:
    description: "E2E DB の DATABASE_URL"
    required: true

runs:
  using: composite
  steps:
    - name: Setup Python
      uses: actions/setup-python@v5
      with:
        python-version: ${{ inputs.python-version }}

    - name: Install uv
      uses: astral-sh/setup-uv@v3
      with:
        enable-cache: true
        cache-dependency-glob: "api/uv.lock"

    - name: Install API dependencies
      shell: bash
      working-directory: api
      run: uv sync --extra dev

    - name: Setup Node
      uses: actions/setup-node@v4
      with:
        node-version: ${{ inputs.node-version }}
        cache: "npm"
        cache-dependency-path: app/package-lock.json

    - name: Install app dependencies
      shell: bash
      working-directory: app
      run: npm ci

    - name: Cache Playwright browsers
      uses: actions/cache@v4
      with:
        path: ~/.cache/ms-playwright
        key: ${{ runner.os }}-playwright-${{ hashFiles('app/package-lock.json') }}
        restore-keys: |
          ${{ runner.os }}-playwright-

    - name: Install Playwright browsers
      shell: bash
      working-directory: app
      run: npx playwright install --with-deps chromium

    - name: Initialize geo_base_e2e schema
      shell: bash
      env:
        PGHOST: ${{ inputs.postgres-host }}
        PGPORT: ${{ inputs.postgres-port }}
        PGUSER: postgres
        PGPASSWORD: postgres
      run: |
        psql -d postgres -c "CREATE DATABASE geo_base_e2e;"
        # docker/postgis-init/01..06 を順に流す
        for f in docker/postgis-init/0[1-6]_*.sql; do
          echo "Applying $f to geo_base_e2e"
          psql -d geo_base_e2e -v ON_ERROR_STOP=1 -f "$f"
        done

    - name: Create E2E admin user
      shell: bash
      working-directory: api
      env:
        DATABASE_URL: ${{ inputs.e2e-database-url }}
        JWT_SECRET: ci-test-secret-not-for-production
      run: |
        uv run python -m lib.auth.cli create-admin \
          --email e2e-admin@test.local \
          --password 'E2E-pass-1!' \
          --name 'E2E Admin' || echo "Admin may already exist; continuing"

    - name: Build Next.js (production)
      shell: bash
      working-directory: app
      env:
        # NEXT_PUBLIC_API_URL は空のまま:
        # ブラウザは /api/* (相対) を叩き、Next.js の rewrites が
        # API_BACKEND_URL に reverse proxy する。これにより
        # HttpOnly refresh cookie がブラウザの localhost:3000 ドメインに保存される。
        API_BACKEND_URL: http://localhost:8000
      run: npm run build
```

- [ ] **Step 2: コミット**

```bash
git add .github/actions/e2e-setup/action.yml
git commit -m "ci: composite action e2e-setup を追加 (#110)

GHA ワークフロー間で共有する E2E セットアップ手順:
uv / npm / Playwright のキャッシュ、geo_base_e2e スキーマ初期化、admin 作成、
Next.js production build までを 1 ステップにまとめる。"
```

---

## Task 17: PR トリガの `e2e-smoke.yml` ワークフローを追加する

**Files:**
- Create: `.github/workflows/e2e-smoke.yml`

- [ ] **Step 1: ワークフローを作成**

`.github/workflows/e2e-smoke.yml` を新規作成:

```yaml
name: E2E smoke (PR)

# Issue #110: PR ごとに smoke 10 件を CI で実行する。
# main マージ時 / nightly の full E2E は Phase 2 で別ワークフロー化する。

on:
  pull_request:
    paths:
      - "app/**"
      - "api/**"
      - "docker/postgis-init/**"
      - ".github/workflows/e2e-*.yml"
      - ".github/actions/e2e-setup/**"

permissions:
  contents: read

concurrency:
  group: e2e-smoke-${{ github.ref }}
  cancel-in-progress: true

jobs:
  smoke:
    name: Run smoke tests
    runs-on: ubuntu-latest
    timeout-minutes: 15

    services:
      postgis:
        image: postgis/postgis:16-3.4
        env:
          POSTGRES_USER: postgres
          POSTGRES_PASSWORD: postgres
          POSTGRES_DB: postgres
        ports:
          - 5432:5432
        options: >-
          --health-cmd pg_isready
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 10s
          --health-timeout 5s
          --health-retries 5

    env:
      DATABASE_URL: postgresql://postgres:postgres@localhost:5432/geo_base_e2e
      JWT_SECRET: ci-test-secret-not-for-production
      E2E_MODE: "1"
      AUTH_PROVIDER: local
      # ブラウザは /api/* 相対で叩き、Next.js rewrites で API へ proxy する。
      # NEXT_PUBLIC_API_URL は空のままにすることで HttpOnly cookie が
      # localhost:3000 ドメインに保存される（同一オリジン）。
      API_BACKEND_URL: http://localhost:8000

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Install psql (client)
        run: sudo apt-get update && sudo apt-get install -y postgresql-client

      - name: Run E2E setup
        uses: ./.github/actions/e2e-setup
        with:
          e2e-database-url: ${{ env.DATABASE_URL }}

      - name: Start FastAPI in background
        working-directory: api
        run: |
          uv run uvicorn lib.main:app --host 0.0.0.0 --port 8000 &
          echo "FASTAPI_PID=$!" >> "$GITHUB_ENV"

      - name: Start Next.js in background
        working-directory: app
        run: |
          npm run start -- --port 3000 &
          echo "NEXTJS_PID=$!" >> "$GITHUB_ENV"

      - name: Wait for FastAPI
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:8000/api/health > /dev/null; then
              echo "FastAPI ready"; exit 0
            fi
            sleep 2
          done
          echo "FastAPI did not become ready in time" >&2
          exit 1

      - name: Wait for Next.js
        run: |
          for i in $(seq 1 30); do
            if curl -fsS http://localhost:3000 > /dev/null; then
              echo "Next.js ready"; exit 0
            fi
            sleep 2
          done
          echo "Next.js did not become ready in time" >&2
          exit 1

      - name: Run smoke tests
        working-directory: app
        env:
          PLAYWRIGHT_API_BASE_URL: http://localhost:8000
          PLAYWRIGHT_BASE_URL: http://localhost:3000
          CI: "true"
        run: npm run test:e2e:smoke

      - name: Upload Playwright report on failure
        if: failure()
        uses: actions/upload-artifact@v4
        with:
          name: playwright-report
          path: app/playwright-report
          retention-days: 7

      - name: Stop background servers
        if: always()
        run: |
          [ -n "${FASTAPI_PID:-}" ] && kill "$FASTAPI_PID" || true
          [ -n "${NEXTJS_PID:-}" ] && kill "$NEXTJS_PID" || true
```

- [ ] **Step 2: ローカルで構文確認**

```fish
# 簡易 lint。act があれば act でも検証できる。
yamllint .github/workflows/e2e-smoke.yml || echo "yamllint not installed; skip"
```

- [ ] **Step 3: コミット**

```bash
git add .github/workflows/e2e-smoke.yml
git commit -m "ci: PR トリガで E2E smoke を実行するワークフローを追加 (#110)

postgis + redis を services で起動し、composite action で setup、
FastAPI と Next.js を background 起動してから smoke 10 件を実行する。
失敗時には Playwright report を artifact としてアップロード。"
```

---

## Task 18: ローカル実行手順の README を書く

**Files:**
- Create: `app/tests/e2e/README.md`

- [ ] **Step 1: README を作成**

`app/tests/e2e/README.md` を新規作成:

```markdown
# E2E テスト（Playwright）

Issue #110 / [E2E 戦略 spec](../../../docs/superpowers/specs/2026-05-10-e2e-testing-design.md)

## ローカル実行手順

### 1. 専用 DB を用意する

初回のみ:

\`\`\`fish
docker compose -f docker/docker-compose.yml up -d
docker compose -f docker/docker-compose.yml exec postgis \
  psql -U postgres -c "CREATE DATABASE geo_base_e2e;"
docker compose -f docker/docker-compose.yml exec postgis bash -c \
  'pg_dump -U postgres --schema-only --no-owner --no-acl geo_base \
     | psql -U postgres -d geo_base_e2e'
\`\`\`

### 2. API を `E2E_MODE=1` で起動する

\`\`\`fish
cd api
set -x DATABASE_URL postgresql://postgres:postgres@localhost:5432/geo_base_e2e
set -x JWT_SECRET test-secret-do-not-use-in-prod
set -x E2E_MODE 1
uv run uvicorn lib.main:app --port 8000
\`\`\`

### 3. Next.js を production で起動する

別ターミナルで:

\`\`\`fish
cd app
# NEXT_PUBLIC_API_URL は空のまま。ブラウザは /api/* を叩き、Next.js rewrites が
# API_BACKEND_URL を reverse proxy 先として使う（HttpOnly cookie の同一オリジン化）。
set -x API_BACKEND_URL http://localhost:8000
npm run build
npm run start -- --port 3000
\`\`\`

### 4. smoke を流す

別ターミナルで:

\`\`\`fish
cd app
npm run test:e2e:smoke
\`\`\`

すべて緑になれば PASS。失敗は `npx playwright show-report` で詳細を確認する。

## ファイル構成

\`\`\`
app/tests/e2e/
├── auth/                 # AUTH-* (unauthenticated project)
├── dashboard/            # DASH-*
├── tilesets/             # TS-*
├── features/             # FT-*
├── datasources/          # DS-*
├── teams/                # TM-*
├── api-keys/             # AK-*
├── fixtures/
│   ├── api-client.ts     # 認証付き request context
│   ├── factories.ts      # API 経由のシードファクトリ
│   └── sample.geojson
├── utils/
│   ├── reset-db.ts       # POST /api/test/reset 呼び出し
│   └── wait-for-server.ts
└── globalSetup.ts        # admin 作成 + login + storageState 保存
\`\`\`

## 新しいテストを書く

1. 該当する feature フォルダ（例: `tilesets/`）に `*.spec.ts` を作成。
2. テスト先頭で `await resetDatabase()` を呼ぶ。
3. 必要なら `factories.ts` の関数で前提データを作成。
4. 認証ありなら `playwright.config.ts` の `authenticated` project に自動マッチする（`auth/` 配下にだけ置かない）。
5. 各テストには `@smoke` タグを付ければ smoke run に含まれる。

## セレクタ規約

優先順位（i18n 対応のため）:

1. `getByRole(...)`
2. `getByLabel(...)`
3. `getByTestId('feature-element-role')` — 命名は `<feature>-<element>-<role>`
4. CSS セレクタは最終手段

テキスト一致 (`getByText`) は将来の英訳で破綻するので、極力使わない。
```

- [ ] **Step 2: コミット**

```bash
git add app/tests/e2e/README.md
git commit -m "docs(app): E2E テストのローカル実行手順とセレクタ規約を追加 (#110)"
```

---

## Task 19: Phase 1 動作の最終確認とドラフト PR 化

- [ ] **Step 1: ブランチをローカルでクリーンに smoke 実行**

すべて止めて、フレッシュな環境で smoke が通ることを確認:

```fish
# DB をリセット
docker compose -f docker/docker-compose.yml exec postgis \
  psql -U postgres -d geo_base_e2e -c "TRUNCATE tilesets, features, datasources, teams, team_members, team_invitations, api_keys, password_reset_tokens, login_attempts RESTART IDENTITY CASCADE;"

# admin 以外を消す。admin 自体は globalSetup で作り直されるので消しても可。

# API 起動
cd api; uv run uvicorn lib.main:app --port 8000 &
# Next.js 起動
cd ../app; npm run build && npm run start -- --port 3000 &

# smoke 実行
cd ../app; npm run test:e2e:smoke
```

期待: 10 件 PASS。

- [ ] **Step 2: 残作業がないか self-check**

- [ ] AUTH-01 / AUTH-04 / DASH-01 / TS-01 / TS-07 / FT-01 / FT-13 / DS-01 / TM-01 / AK-01 が全て `@smoke` タグ付きで存在する
- [ ] `factories.ts` に `createTileset` / `createDatasource` / `createTeam` / `createFeature` / `createApiKey` が揃っている
- [ ] `playwright.config.ts` の `authenticated` / `unauthenticated` project が機能している
- [ ] `POST /api/test/reset` は `E2E_MODE=1` のときだけ動く
- [ ] `e2e-smoke.yml` ワークフローが存在する
- [ ] `app/tests/e2e/README.md` が存在し、ローカル手順とセレクタ規約が書かれている

- [ ] **Step 3: push してドラフト PR を起票**

```bash
git push -u origin feat/issue-110-e2e-phase1
gh pr create --draft --title "feat: E2E Phase 1 — Foundation + smoke + PR CI (#110)" --body "$(cat <<'EOF'
## Summary
- Playwright を `app/` に導入し、smoke 10 件を実装した
- `POST /api/test/reset` を `E2E_MODE=1` ガード + DB 名チェックで提供
- `cmd_create_admin` に `--password` / `--name` フラグを追加（非対話実行用）
- composite action `e2e-setup` と PR トリガの `e2e-smoke.yml` を追加

Closes #110

## Test plan
- [ ] ローカルで `npm run test:e2e:smoke` が 10/10 で PASS する
- [ ] `E2E_MODE` 未設定で `/api/test/reset` が 404 を返す
- [ ] `DATABASE_URL` を `geo_base_test` に向けたら `/api/test/reset` が 400 で abort する
- [ ] 本 PR の Actions 上で `E2E smoke (PR)` ワークフローがグリーンになる

## Spec / 関連
- Spec: `docs/superpowers/specs/2026-05-10-e2e-testing-design.md`
- Plan: `docs/superpowers/plans/2026-05-10-e2e-phase1.md`
- Epic: #113
EOF
)"
```

期待: GitHub の PR ページが返る。`E2E smoke (PR)` ワークフローが起動して緑になることを確認する。

- [ ] **Step 4: CI のグリーンを確認**

PR ページの Checks タブで `E2E smoke (PR)` が PASS していること。失敗したら artifact から Playwright report をダウンロードして原因究明 → 修正 → 再 push。

- [ ] **Step 5: Issue #110 のチェックリストを更新する**

PR がレビュー可能になったら、Issue #110 の本文の対応するチェックボックスを順次オンにし、PR 番号を子コメントとしてリンクする:

```bash
gh issue comment 110 --body "Phase 1 implementation PR: #<PR-number> (draft)"
```

---

## ファイル全体マップ（参照用）

| 種別 | パス |
|---|---|
| Spec | `docs/superpowers/specs/2026-05-10-e2e-testing-design.md` |
| Plan | `docs/superpowers/plans/2026-05-10-e2e-phase1.md`（このファイル） |
| API | `api/lib/auth/cli.py`（修正） |
| API | `api/lib/main.py`（修正） |
| API | `api/lib/routers/test_helpers.py`（新規） |
| API | `api/tests/test_auth_cli.py`（新規） |
| API | `api/tests/test_test_helpers.py`（新規） |
| Docker | `docker/postgis-init/98_create_e2e_db.sh`（新規） |
| Docker | `docker/docker-compose.yml`（コメント追記） |
| App | `app/playwright.config.ts`（新規） |
| App | `app/package.json`（修正） |
| App | `app/.gitignore`（追記） |
| App | `app/src/app/login/page.tsx`（data-testid 追加） |
| App | `app/src/app/page.tsx`（data-testid 追加） |
| App | `app/src/app/tilesets/page.tsx`、`app/src/app/tilesets/new/page.tsx`（data-testid 追加） |
| App | `app/src/app/features/page.tsx`、`app/src/app/features/import/page.tsx`（data-testid 追加） |
| App | `app/src/app/datasources/page.tsx`（data-testid 追加） |
| App | `app/src/app/teams/page.tsx`（data-testid 追加） |
| App | `app/src/app/api-keys/page.tsx`（data-testid 追加） |
| App | `app/src/components/layout/sidebar.tsx`（data-testid 追加） |
| Tests | `app/tests/e2e/globalSetup.ts`（新規） |
| Tests | `app/tests/e2e/utils/wait-for-server.ts`（新規） |
| Tests | `app/tests/e2e/utils/reset-db.ts`（新規） |
| Tests | `app/tests/e2e/fixtures/api-client.ts`（新規） |
| Tests | `app/tests/e2e/fixtures/factories.ts`（新規） |
| Tests | `app/tests/e2e/fixtures/sample.geojson`（新規） |
| Tests | `app/tests/e2e/auth/login.spec.ts`（新規） |
| Tests | `app/tests/e2e/dashboard/dashboard.spec.ts`（新規） |
| Tests | `app/tests/e2e/tilesets/list-create.spec.ts`（新規） |
| Tests | `app/tests/e2e/features/list.spec.ts`（新規） |
| Tests | `app/tests/e2e/features/import.spec.ts`（新規） |
| Tests | `app/tests/e2e/datasources/list.spec.ts`（新規） |
| Tests | `app/tests/e2e/teams/list.spec.ts`（新規） |
| Tests | `app/tests/e2e/api-keys/list.spec.ts`（新規） |
| Tests | `app/tests/e2e/README.md`（新規） |
| CI | `.github/actions/e2e-setup/action.yml`（新規） |
| CI | `.github/workflows/e2e-smoke.yml`（新規） |
